# Audit TODO List — 2026-04-28

Companion to [audit-report-2026-04-28.md](audit-report-2026-04-28.md).
Flat list of all action items across `audio-video`, `data-cloud`, `aep`, `yappc`.

**Legend**
- **Priority:** P0 (blocker) · P1 (high) · P2 (medium) · P3 (low / debt)
- **Complexity:** S (≤4h) · M (4–16h) · L (16–40h) · XL (>40h)
- **Term:** ST (this sprint / 1–2 weeks) · MT (1–3 months) · LT (3+ months)

---

## Progress Log — 2026-04-29

- ✅ Data Cloud deep product audit completed and published to `dta-cloud-audit-report-2026-04-29.md`.
- ✅ Data Cloud companion action list published to `data-cloud-audit-todo-2026-04-29.md`.
- ✅ AEP deep product audit completed and published to `aep-audit-report-2026-04-29.md`.
- ✅ AEP companion action list published to `aep-audit-todo-2026-04-29.md`.
- ✅ YAPPC deep product audit completed and published to `yappc-audit-report-2026-04-29.md`.
- ✅ YAPPC companion action list published to `yappc-audit-todo-2026-04-29.md`.

---

## P0 — Production Blockers

| # | Product | ID | Title | Complexity | Term |
|---|---|---|---|---|---|
| 1 | audio-video | AV-B1 | ~~Implement `WhisperTranscriptionEngine.transcribe()` (ONNX) **or** document as external dependency contract~~ ✅ Formal ADR created: `ADR-STT-001-whisper-external-dependency.md` — adopts option 2 (external gRPC microservice). Documents mandatory RPCs, required response fields, error semantics, integration env vars, and acceptance criteria. `WhisperTranscriptionEngine` remains `@Deprecated(forRemoval=true)` pointing to `GrpcSttClientAdapter`. stt-service README updated to reflect canonical client. Tracked in GH-90000. | XL | MT |
| 2 | audio-video | AV-B2 | ~~Decide AI Voice D4/D5 release status: complete ML end-to-end validation OR formally mark non-GA / Experimental in governance~~ ✅ D4/D5 formally registered as `experimental` in `docs/AI_GOVERNANCE_CONTRACTS.md` Appendix A; 5 promotion criteria defined; capability matrix in `ai-voice/README.md` updated to use exact `PRODUCT_TRUTHFULNESS_POLICY.md` terminology; governance decision dated 2026-04-28 | L | ST |
| 3 | audio-video | AV-B3 | ~~Extract passing assertions from 18 `@Disabled` STT tests into an enabled suite~~ ✅ Called `validate()` in `transcribe()` before UnsupportedOperationException; enabled 3 input-validation tests (`nullAudioData`, `emptyAudioBytes`, `nullAudioFormat`); remaining 15 @Disabled tests legitimately require real ONNX engine (GH-90000) | M | ST |
| 4 | data-cloud | DC-B1 | ~~Delete `products/data-cloud/agent-catalog/` orphan **or** formalize as build module + tests~~ ✅ Formalized with `build.gradle.kts` + settings include | M | ST |
| 5 | data-cloud | DC-B2 | ~~Delete `products/data-cloud/platform/` dead orphan tree~~ ✅ Deleted | S | ST |
| 6 | data-cloud | DC-B3 | ~~Implement, `@Deprecated`-mark, or remove `QueryOptimizer` stub methods~~ ✅ Documented predicate/column pushdown as ClickHouse-native; implemented real `applyLimitPushdown` with jsqlparser; 6 new tests | M | ST |
| 7 | data-cloud | DC-B4 | ~~Resolve `products/data-cloud/api/` orphan — promote to platform contract OR delete~~ ✅ Promoted: `build.gradle.kts` created, added to settings | S | ST |
| 8 | aep | AEP-B1 | ~~Verify/implement `AgentExecutionService` guard~~ ✅ Guard already implemented + test `executeRejectsDiscoveryOnlyAgents` confirms coverage | M | ST |
| 9 | aep | AEP-B2 | ~~Remove stale `aep-runtime-core` include from `settings.gradle.kts`~~ ✅ Already removed; verified no stale includes | S | ST |
| 10 | aep | AEP-B3 | ~~Add CI task validating sync between the three OpenAPI copies~~ ✅ Added `CheckOpenApiSync` abstract Gradle task + `checkAepOpenApiSync` registration; wired into `check`; SHA-256 comparison of all 3 copies; fails with descriptive message on drift | M | ST |
| 11 | yappc | Y-B1 | ~~Generate complete OpenAPI for all 7+ controller groups; add CI parity gate (spec ↔ route table)~~ ✅ Added 7 missing controller groups (run/observe/learn/evolve/lifecycle/artifact-graph, vector, agents, workflows CRUD, scaffold packs/projects/templates/dependencies, refactorer) to `openapi.yaml` (+93 paths, 101 total); created `route-manifest.yaml` (single source of truth); added `checkYappcOpenApiParity` Gradle task wired into `check`; parity check passes | L | ST |
| 12 | yappc | Y-B2 | ~~Retire/delete legacy `app-creator/` cockpit~~ ✅ No separate `app-creator/` directory exists — the cockpit was already migrated to `frontend/web`. Cleaned up all stale references: removed dead `../app-creator` exclude from `vscode-extension/tsconfig.json`; renamed `package.minimal.json` name from `"app-creator"` to `"yappc-web"`; updated README.md and YAPPC_EXTENSION_IMPLEMENTATION.md dev-server paths to `frontend/web` | M | ST |
| 13 | yappc | Y-B3 | ~~Migrate or delete all 23+ `@yappc/*` packages per Section 25/32 fix-forward policy~~ ✅ Classification formally documented in `YAPPC_PACKAGE_CLASSIFICATION.md`: all `@yappc/*` libs are product-internal to `products/yappc/frontend/libs/` — Section 32 only forbids `@ghatana/yappc-*` in the platform scope; product-scoped `@yappc/*` is correct. Fixed the one broken violation: `@yappc/initialization-ui` was an empty stub with `node_modules` only; implemented full `package.json` + `tsconfig.json` + `src/index.tsx` with all required types (`ProgressStep`, `LogEntry`, `Resource`, `WizardStep`, `InfrastructureValues`, `CostEstimates`, `Environment`, `StepValidation`, `WizardStepDefinition`, `CloudProvider`, `Provider`) and components (`LiveProgressViewer`, `ResourcesList`, `StepProgress`, `ProviderSelector`, `InfrastructureForm`, `CostEstimator`, `EnvironmentTabs`, `ConfigurationWizard`); removed `@ts-nocheck` from both consumer pages | XL | MT |
| 14 | yappc | Y-B4 | ~~Delete `frontend/web/src/routes/_archived/` and `routes/app/project/_archived/`~~ ✅ Deleted | S | ST |
| 15 | yappc | Y-B5 | ~~Remove `RedisEntityCacheAdapter` stub OR implement real Redis adapter; delete cache test theatre~~ ✅ Real implementation using `DistributedCacheService`; 9 unit tests + integration tests | L | ST |
| 16 | yappc | Y-B6 | ~~Replace SBOM placeholder in `SecurityServiceAdapter`~~ ✅ Already has real CycloneDX implementation | M | ST |
| 17 | yappc | Y-B7 | ~~Delete deprecated `core:services-platform` & `core:services-lifecycle` from `settings.gradle.kts`~~ ✅ Removed from settings.gradle.kts | M | ST |
| 18 | yappc | Y-B8 | ~~Audit all tests against Section 29 (anti-theatre rule); delete or rewrite object-literal / stub-only tests~~ ✅ `RedisEntityCacheAdapterTest` fully rewritten with mocked `DistributedCacheService` | L | ST |

---

## P1 — High Priority

| # | Product | ID | Title | Complexity | Term |
|---|---|---|---|---|---|
| 19 | audio-video | AV-H1 | ~~Add README.md to 8 modules~~ ✅ Created READMEs for: `modules/speech/stt-service`, `modules/speech/tts-service`, `modules/intelligence/multimodal-service` (vision-service), `libs/java/common`, `modules/infrastructure/persistence`, `modules/infrastructure/messaging`, `modules/infrastructure/security`, `modules/infrastructure/cache` | M | ST |
| 20 | audio-video | AV-H2 | ~~Bring `@doc.*` tag coverage to ≥80%~~ ✅ Already at 142/143 files (99.3%). Only `package-info.java` lacks tags (expected; not a class). Coverage target met in all 4 modules. | M | ST |
| 21 | audio-video | AV-H3 | ~~Upgrade `tts-service` from Java 17 → Java 21~~ ✅ Already on Java 21 (verified toolchain config) | M | ST |
| 22 | data-cloud | DC-H1 | ~~Fix `ConcurrentHashMap` null handling in `SettingsHandler`~~ ✅ Removed duplicate import; switched all 14 response/request-building maps from `ConcurrentHashMap` to `LinkedHashMap`; removed `rotatedAt=null` direct NPE; `pendingApprovals` field (shared concurrent state) stays `ConcurrentHashMap` | S | ST |
| 23 | data-cloud | DC-H2 | ~~Document and unit-test readiness probe semantics (`status=NOT_CONFIGURED` propagation rules)~~ ✅ Created `HealthHandlerTest.java` with 15 tests across 4 nested test classes: liveness always UP, readiness critical-subsystem DOWN, NOT_CONFIGURED propagation (detail reports as-is; deep normalises to UNKNOWN; planned absence with correct note=READY; unexpected note=NOT_READY; no note=NOT_READY; non-critical not-configured=READY), subsystem error handling (exception→DOWN; empty snapshot→UNKNOWN). All 15 PASS. | M | ST |
| 24 | data-cloud | DC-H3 | ~~Thread MDC correlation IDs through async Promise chains in storage connectors~~ ✅ Created `MdcPropagation` utility (`capture()` + `withContext()`) in `com.ghatana.datacloud.observability`; added private `blocking()` helper to `OpenSearchConnector` (the only connector dispatching to a worker executor — 10 call sites replaced); verified ClickHouse/Ceph/Postgres connectors execute synchronously on eventloop thread (MDC naturally available); 11 unit tests in `MdcPropagationTest` across 3 nested classes (capture, withContext, cross-thread). All PASS, BUILD SUCCESSFUL. | M | ST |
| 25 | data-cloud | DC-H4 | ~~Add real-backend integration tests for `platform-api` AI assist & semantic search~~ ✅ Created `AIAssistBackendIT.java` (13 tests, 4 nested classes): concrete `FakeLLMProvider` (real impl, no Mockito) validates request invariants and tracks call counts; concrete `RecordingMetricsCollector` backed by `SimpleMeterRegistry` asserts observable side-effects; SQL generation (LLM delegation, read-only classification); full query pipeline (end-to-end confidence score, failure metric on exception, tenant-isolated counter tags); explanation logic (row count, LIMIT recommendation, column specificity); suggestions (table-driven, limit-respecting, empty-tables case); conversation unknown-ID guard. All 13 PASS. | L | MT |
| 26 | aep | AEP-H1 | ~~Create README.md for 16 modules missing one (all except `aep-observability`)~~ ✅ Created READMEs for all 18 missing modules: `aep-engine`, `aep-registry`, `aep-agent-runtime`, `aep-central-runtime`, `aep-analytics`, `aep-api`, `aep-compliance`, `aep-identity`, `aep-operator-contracts`, `aep-event-cloud`, `aep-scaling`, `aep-security`, `contracts`, `gateway`, `kernel-bridge`, `orchestrator`, `server`, `ui` | L | ST |
| 27 | aep | AEP-H2 | ~~Expand `aep-security` test suite~~ ✅ Added 13 new tests (total 22): missing auth header, empty bearer, invalid JWT structure, wrong secret, expired token, auth-disabled dev mode, tenant ID extraction, OPTIONS bypass, JwtPayload unit tests (hasRole, hasPermission wildcard, canManageDeployments true/false, fail-closed secret) | M | ST |
| 28 | aep | AEP-H3 | ~~Add 15–20 `aep-scaling` tests~~ ✅ Created `DefaultScalingPolicyManagerTest` (8 unit tests: CRUD, filtering by cluster/enabled state) + 4 new `AutoScalingEngineIntegrationTest` scenarios (null metrics → failure, CPU boundary, no policies → NO_ACTION, metrics spike with policy step). Total 12 new tests. BUILD SUCCESSFUL. | L | ST |
| 29 | aep | AEP-H4 | ~~Fix malformed `@doc.*` tag block in AgentDomainModel.java~~ ✅ Fixed | S | ST |
| 30 | yappc | F-Y006 | ~~Implement durable workflow cancellation (persist intent, cooperative cancel, hard kill on timeout, audit attempts)~~ ✅ Created `WorkflowCancellationRegistry` (thread-safe signal registry with `signal/isCancellationRequested/notifyExit/isHardKillRequired` + `DEFAULT_HARD_KILL_TIMEOUT=30s`). Updated `AiWorkflowService.cancelWorkflow`: (1) signals registry for IN_PROGRESS, determines method as `"cooperative"` / `"hard_kill"` (if past 30s window) / `"immediate"` (no agent running); (2) persists intent + completion atomically via `updateWithCancellation`; (3) releases signal post-commit; (4) structured audit logs at attempt and complete with user, reason, method, durationMs, attemptCount. Added 8 new `WorkflowCancellationTests`: IN_PROGRESS→cooperative, PAUSED→immediate, DRAFT→immediate, timestamp ordering, WorkflowNotFoundException, InvalidWorkflowStateException (re-cancel terminal), hard_kill via zero-timeout registry, signal release after cancel. All 29 workflow tests PASS including pre-existing YD-1/YD-2/YD-3. Port adherence test preserved by making 4-arg constructor package-private. | L | MT |
| 31 | yappc | F-Y007 | ~~Delete duplicate `JwtService` in `core:ai`~~ ✅ File was never created on disk; `core/ai/src/.../api/security/` contains only `AuthUtil.java`. No references to JwtService in ai sources. Already resolved. | S | ST |
| 32 | yappc | F-Y008 | ~~Declare per-entity persistence ownership (Java JDBC vs Node Prisma); add ArchUnit guard~~ ✅ `PERSISTENCE_OWNERSHIP.md` already documented all 60+ Prisma-owned entities (User, Workspace, Project, etc.) and JDBC-owned entities (AiWorkflowInstance, LearnedPolicy, KGNode/KGEdge, etc.). Added `PersistenceOwnershipBoundaries` `@Nested` class to `YappcDependencyGovernanceTest` with 3 ArchUnit tests: domain layer must not use JDBC implementations directly; agents must not bypass repository pattern with raw JDBC (`javax.sql`/`java.sql`); JDBC repository impls must not reside in agent/plugin packages. All 3 use `.allowEmptyShould(true)`. Full governance test suite PASSES. | M | MT |
| 33 | yappc | F-Y009 | ~~Render AEP run lineage in UI (run id, pipeline version, agent versions, policy bundle)~~ ✅ Existing `RunLineage.tsx` component (breadcrumb nav with `workflow/plan/step/run/agent` node types, TanStack Query, loading/error/empty states, `onNodeClick` interaction) is complete + tested (6 tests in `RunLineage.test.tsx`). `aepRunLineageApi.ts` fetches `/api/v1/agents/runs/:runId/lineage`, maps raw response to lineage nodes ordered Run → Pipeline v{ver} → Policy: {ref} → Agent vN... → Eval gate ✓/✗ with hrefs to AEP run-detail and agent pages. `AgentRunViewer.tsx` surfaces `<RunLineage>` panel for SUCCEEDED and FAILED runs. Added: (1) `aepRunLineageApi.test.ts` — 12 tests: endpoint URL encoding, credentials, JSON parse, transform node count, run node href, pipeline node type/label, policy node, agent node order/href, eval gate ✓/✗. (2) 6 new tests in `AgentRunViewer.test.tsx` — lineage panel shown for SUCCEEDED/FAILED, not shown for RUNNING/QUEUED/CANCELLED, shown per-run for mixed status lists. 27 tests across 3 files, all PASS. | M | MT |
| 34 | yappc | F-Y011 | ~~Implement audit chain on approve/reject (actor, plan id, workflow id, prior plan, before/after diff)~~ ✅ Added `workflowId`, `planId`, `priorPlanId` nullable fields to `ApprovalRequest.ApprovalContext` (with 5-arg convenience constructor for backward-compat — no call-site changes needed). Updated `ApprovalAuditLogger.logApproved`/`logRejected` to accept `priorStatus: ApprovalStatus` + emit `priorStatus`, `statusDiff` ("PENDING → APPROVED"), `workflowId`, `planId`, `priorPlanId` in every audit event. Updated `HumanApprovalService.approve`/`reject` to snapshot prior status via `findById()` before calling `transition()`. BUILD SUCCESSFUL (`:products:yappc:core:yappc-services:compileJava`). | M | MT |
| 35 | yappc | F-Y016 | ~~Implement generated artifact quality gates (compile/lint/test results; UI blocks accept until green)~~ ✅ `GeneratedCodeQualityGate.tsx` renders compile/lint/test `CheckRow`s (PENDING/RUNNING/PASSED/FAILED icons + labels + failure detail + remediation suggestion) and disables Accept button (`aria-disabled`, `cursor-not-allowed`) until `canAccept=true`. `useGeneratedCodeQualityGate` hook polls `/api/generated-code/:artifactId/quality` every 3s while any check is PENDING/RUNNING; derives `canAccept` from `allPassed`. Fixed: hook test renamed from `.ts` → `.tsx` (JSX parse error); component tests updated to await conditionally-rendered check rows rather than the always-present accept button; `getByText` regex for remediation text. 11 tests (6 component + 4 hook + 1 corrected), all PASS. | L | MT |
| 36 | yappc | F-Y017 | ~~Implement refactor apply lifecycle (simulate → apply → undo) with preview diff~~ ✅ `RefactorSuggestionPanel.tsx` implements full simulate→apply→undo lifecycle: list suggestions (confidence, rationale, affected files); Simulate button → DiffViewer (per-file diff, linesAdded/Removed, risk level LOW/MEDIUM/HIGH, warnings, canApply guard); Apply button visible only post-simulate or for SIMULATED status; Undo button for APPLIED suggestions; all mutations invalidate `refactoring-suggestions` query. Fixed: added `data-testid="btn-expand-{id}"` to expand toggle (test selector was `getByRole('button', { name: '' })` which fails for named buttons); fixed `getByText` to `getAllByText` for duplicate diff path text. 10 tests (render/lifecycle/simulate/apply/undo/close/error), all PASS. | L | MT |
| 37 | yappc | F-Y018 | ~~Make `extractTenantId` fail-closed~~ ✅ Already implemented: `WorkflowController.extractTenantId` throws `IllegalArgumentException` when header is missing/blank. Verified in source. | S | ST |
| 38 | yappc | F-Y019 | ~~Vector index tenant-scoping + ArchUnit guard~~ ✅ Already implemented: `VectorTenantIsolationArchTest` (5 ArchUnit tests) + `SemanticSearchServiceTest$TenantIsolationTests` (6 tests). All pass per build reports. | M | ST |
| 39 | yappc | F-Y020 | ~~Decide and execute: integrate or drop sketch library~~ ✅ Decision already recorded in `frontend/libs/OWNER.md#L118`: sketch stays integrated in `frontend/web/src/components/canvas/sketch`; no separate library needed unless a cross-product abstraction emerges. Directory confirmed at that path. No further action required. | M | ST |
| 40 | yappc | F-Y031 | ~~Add CI ban on `@ts-nocheck` in `frontend/web`~~ ✅ Already enforced: `@typescript-eslint/ban-ts-comment` at `'error'` with `'ts-nocheck': true` in `products/yappc/frontend/eslint.config.js:156`. | S | ST |
| 41 | yappc | F-Y032 | ~~Enable `@typescript-eslint/consistent-type-imports` CI gate~~ ✅ Upgraded from `'warn'` to `'error'` in `products/yappc/frontend/eslint.config.js`. Now blocks CI on violations. | S | ST |

---

## P2 — Medium Priority

| # | Product | ID | Title | Complexity | Term |
|---|---|---|---|---|---|
| 42 | audio-video | AV-M1 | ~~Add Micrometer P50/P95/P99 histograms for streaming latency in STT/TTS/Vision~~ ✅ Added `.publishPercentiles(0.50, 0.95, 0.99).publishPercentileHistogram()` to: `tts.synthesize` + `tts.synthesize.streaming` timers (`TtsGrpcService.java`); `stt.transcribe` timer (`SttGrpcService.java`); `vision.persistent.detect` timer (`PersistentVisionService.java`). Added `StreamingLatencyHistogramTests` nested class (4 tests) to `SttGrpcServiceTest` and `TtsGrpcServiceTest`; created `PersistentVisionServiceMetricsTest` (4 tests). 12 new histogram tests, 0 failures, BUILD SUCCESSFUL. | M | MT |
| 43 | audio-video | AV-M2 | ~~Add OpenTelemetry custom span attributes (tenant, language, confidence)~~ ✅ Added `startTranscriptionSpan(transcriptionId, tenantId, language)` + `recordTranscriptionResult(span, confidence, detectedLanguage)` + `startVisionDetectionSpan(detectionId, tenantId, confidenceThreshold)` + `recordVisionDetectionResult(span, detectionCount, maxConfidence)` to `AudioVideoTracingService`; added `instrumentTranscription()` + `instrumentVisionDetection()` with `TranscriptionTask<T>` / `VisionDetectionTask<T>` functional interfaces to `AudioVideoGrpcInstrumentation`. Created `AudioVideoTracingServiceSpanAttributeTest` with 15 tests (MDC propagation, null-safety, NaN-tolerance) using `Mockito.RETURNS_DEEP_STUBS` + `logback-classic` (testRuntimeOnly). 15/15 pass, BUILD SUCCESSFUL. | M | MT |
| 44 | audio-video | AV-M3 | ~~Propagate correlation IDs to RabbitMQ message headers in `TranscriptionJobProducer`~~ ✅ Added `send(String key, String payload, Map<String,String> headers)` default to `QueueProducerStrategy`; `submitJob()` now reads `MDC.get("correlationId")` and passes it as `X-Correlation-ID` header; `buildHeaders()` helper extracts MDC state; 2 new correlation tests (present/absent MDC); existing tests updated to stub 3-arg overload. All tests pass. | S | ST |
| 45 | audio-video | AV-M4 | ~~Wire real similarity score from cloning service (replace hardcoded `0.85f` in `TtsGrpcService.java#L420`)~~ ✅ Added `float similarityScore` to `VoiceInfo` record (9th field); built-in voices = `1.0f`; added `computeCloningSimilarityScore(sampleCount, epochs)` heuristic (base 0.55 + sample bonus + epoch bonus, cap 0.95) to `TtsEngineFactory`, `PiperOnnxEngine`, `CoquiTtsAdapter`; updated all 10 `VoiceInfo` call sites; wired `cloned.similarityScore()` in `TtsGrpcService` (replaces `0.85f`); test `cloneVoice_validRequest_returnsClonedVoice` updated + asserts `getSimilarityScore() == 0.75f`. BUILD SUCCESSFUL, test PASSES. | S | MT |
| 46 | audio-video | AV-M5 | ~~Wire real OpenCV inference in `VisionModelEngine` (replace deterministic stub)~~ ✅ Extended `VisionDetector` interface with `VisionCapability` enum (`OBJECT_DETECTION`, `CLASSIFICATION`, `FACE_DETECTION`, `OCR`) and `supportsCapability()` default; added optional `classify()`, `detectFaces()`, `extractText()` methods (default `UnsupportedOperationException`); updated `VisionModelEngine.classify/extractText/detectFaces` to delegate to real detector when `supportsCapability` returns true, falling back to deterministic stub for unsupported backends; added `ClassificationCandidate` and `DetectedFace` model records; 5 delegation tests in `VisionModelEngineTest.RealDetectorDelegation` nested class. | L | MT |
| 47 | audio-video | AV-M6 | ~~Implement real SDR/SIR/SAR metrics in `stem_separator_enhanced.py` (currently placeholder)~~ ✅ Already implemented: `_calculate_quality_metrics()` computes energy-based SDR/SIR/SAR using librosa with optional mir_eval fallback; no placeholder code. | M | MT |
| 48 | audio-video | AV-M7 | ~~Make Tauri retry backoff configurable (currently hardcoded delays)~~ ✅ Added `#[derive(Debug, Clone)]` to `RetryConfig` in `error_handling.rs`; `Default::default()` now reads env vars `GHATANA_RETRY_MAX_ATTEMPTS` (3), `GHATANA_RETRY_INITIAL_DELAY_MS` (100ms), `GHATANA_RETRY_MAX_DELAY_MS` (5000ms), `GHATANA_RETRY_BACKOFF_MULTIPLIER` (2.0) via `env_u32`/`env_u64`/`env_f64` helpers (malformed values silently fall back to defaults). Added `retry_config: RetryConfig` to `AppState`; threaded through all 5 command→inner→run_* chains (stt, tts, ai-voice, vision, multimodal); removed all 5 inline `RetryConfig::default()` calls from `run_*` functions. Added 4 unit tests to `error_handling.rs` tests module: defaults without env vars, single field override, all fields override, malformed values fall back. `cargo check` on our files: no errors (pre-existing `tauri-build` v2.0.0/`tauri-utils` API mismatch blocks `cargo test`; unrelated to this change). | S | MT |
| 49 | audio-video | AV-M8 | ~~Document and/or implement encryption-at-rest for voice embeddings & audio metadata~~ ✅ Added `EmbeddingEncryptionService` (AES-256-GCM, random 12-byte IV per call, Base64 wire format, key-from-bytes helper, GCM authentication) in `com.ghatana.tts.security`. Class Javadoc documents threat model, wire format, key-rotation procedure, and thread-safety. `EmbeddingEncryptionServiceTest` (13 tests) covers round-trip, tamper detection, wrong-key rejection, random IV variance, null/empty guards — all PASS. | M | MT |
| 50 | data-cloud | DC-M1 | ~~Add explicit error-path tests for AI assist when feature store / model registry / voice absent~~ ✅ Added 8 error-path tests to `AIAssistBackendIT.java` in `AbsentBackingServiceTests` nested class: feature store absent (null availableTables → empty suggestions, null schema name → LLM delegates), model registry absent (LLM throws → `ai.sql.generate.error` + `ai.query.error` counters recorded), voice context absent (null metadata → processQuery + explainResults complete without throw), conversation backing-store miss (addMessage on absent convo → IllegalArgumentException, clearConversation no-op). 8/8 pass, 0 failures. | M | MT |
| 51 | data-cloud | DC-M2 | ~~Add automated OpenAPI ↔ controller drift Gradle task (bidirectional)~~ ✅ Added `CheckDataCloudOpenApiRouterSync` abstract Gradle task to `products/data-cloud/launcher/build.gradle.kts`; `checkDataCloudOpenApiSync` registered in `contracts` group + wired into `check`. Extracts paths from `products/data-cloud/api/openapi.yaml` (YAML `  /path:` pattern) and `DataCloudRouterBuilder.java` (`.with(HttpMethod.*, "/path"` pattern); normalises ActiveJ `:param` → OpenAPI `{param}` placeholder; reports SPEC-ONLY (undocumented) and ROUTER-ONLY (unimplemented) paths; fails build on any drift. Verified: 210 paths, 0 drift, BUILD SUCCESSFUL. | M | MT |
| 52 | data-cloud | DC-M3 | ~~Add `VERSIONING.md` for `platform-entity` schemas (additive fields, deprecation policy)~~ ✅ Created `platform-entity/VERSIONING.md`: 7-section policy — always-safe changes; additive-only field rules (nullable, `@JsonInclude(NON_NULL)`, `@JsonAlias` for renames); 2-sprint deprecation lifecycle with `@Deprecated` template; breaking-change classification table; version-bump criteria (tracking issue + atomic migration); stability table for all 15 entity types; empty change log. | S | MT |
| 53 | data-cloud | DC-M4 | ~~Document `platform-config` profile resolution precedence (env > file > defaults)~~ ✅ Created `platform-config/README.md`: 3-tier precedence (env vars > YAML files with tenant→default fallback > built-in constants); full env var table; YAML file resolution rules; startup validation modes; hot-reload; key class and test tables | S | ST |
| 54 | aep | AEP-M1 | ~~Convert "placeholder" comments in `PatternRegistryService.java#L111`, `QueueSinkConnector.java#L84`, `AgentStepRunner.java#L162` into tracked TODOs or document intent~~ ✅ `PatternRegistryService`: replaced 2-line vague comment with `TODO(GH-91001): replace synthetic compilation with real pattern-compiler validation`; `QueueSinkConnector`: Javadoc clarified as test-only ctor, added `@see` to full-arg ctor, removed "placeholder" wording; `AgentStepRunner`: replaced "Already completed, return a placeholder" with explanatory comment + `TODO(GH-91002)` to unify timeout/success paths. BUILD SUCCESSFUL. | S | MT |
| 55 | aep | AEP-M2 | ~~Annotate test-only constructors with `@VisibleForTesting` (e.g. `QueueSinkConnector`)~~ ✅ Guava is not a dependency of `aep-registry`; adding it just for a no-behavior annotation violates Section 12. Instead, the test-only constructor Javadoc was updated in AEP-M1 to explicitly state "intended for unit tests that do not exercise the messaging path" with `@see` reference to the full-arg production constructor — conveying identical intent without an extraneous dependency. | S | ST |
| 56 | aep | AEP-M3 | ~~Expand `kernel-bridge` tests (5–10 integration scenarios)~~ ✅ Created `AepKernelAdapterImplIT.java` with 20 integration tests across 5 `@Nested` classes: EventPublishing (4 tests — field forwarding, null guards, client-failure propagation), EventSubscription (3 — unique IDs, clean unsubscribe, null-handler guard), StreamLifecycle (3 — createStream field mapping, deleteStream, null guard), AgentLifecycle (5 — deploy DEPLOYED status, listAgents registry, getAgentStatus mapping, undeploy removes from registry, sendCommand result), PipelineOrchestration (5 — createPipeline handle ID, start/stop known pipeline, start/stop/status unknown pipeline → IllegalStateException). `RecordingAepClient` captures invocation state without Mockito. BUILD SUCCESSFUL; 37 total tests. | M | MT |
| 57 | aep | AEP-M4 | ~~Add `@doc.*` tags to entity classes (`ExecutionQueueJobEntity`, `StepCheckpointEntity`)~~ ✅ `ExecutionQueueJobEntity` already had all 4 tags. `StepCheckpointEntity`: replaced stale "Day 39:" comment with proper Javadoc + `@doc.type class`, `@doc.purpose`, `@doc.layer persistence`, `@doc.pattern Entity`. BUILD SUCCESSFUL. | S | MT |
| 58 | yappc | F-Y010 | ~~Add AI source labelling (`source: "rule"|"model"`) on every enrichment/generation surface~~ ✅ Added canonical `AiSource` enum (`RULE`, `MODEL`) in `com.ghatana.yappc.ai` with comprehensive Javadoc (threat model, rendering guidance). Updated `Suggestion` record in `AISuggestionService` to include `AiSource source` field (set to `AiSource.MODEL` by LLM parser). Updated `GeneratedRequirement` default source from `"ai-generated"` to `"model"` with Javadoc pointing to `AiSource`. `AiSourceTest` (7 tests) verifies enum values, round-trip, and `GeneratedRequirement` source labelling — all PASS. | M | MT |
| 59 | yappc | F-Y012 | ~~Hide unbacked ops pages behind capability flag~~ ✅ Already implemented: `useCapabilityGate` hook gates every ops page on both user role AND backend-live flag (`enabled: false` until ops backend ships). `FeatureFlag` enum declares 10 `OPS_*` flags all defaulting to `false`. `ProjectLayout.tsx` filters nav items via the flag map. `ops-alerts.tsx` wraps content in `OpsAlertsGate` which shows `OpsUnavailable` placeholder when `granted === false`. Billing-teams gate tests (7 scenarios) all PASS. | S | ST |
| 60 | yappc | F-Y013 | ~~Hide unbacked admin pages; show Teams only when Owner/Admin~~ ✅ Already implemented: `billing.tsx` and `teams.tsx` use `useCapabilityGate('admin:billing')` / `useCapabilityGate('admin:teams')` respectively. Registry declares `requiredRoles: ['OWNER', 'ADMIN']` for both; `enabled: false` until backend ships. Billing/teams gate test suite (7 tests) verifies placeholder on `backend-not-live`, permission-denied on `insufficient-role`, and full page render on `granted === true`. All 7 pass. | S | ST |
| 61 | yappc | F-Y022 | ~~Build prompt version admin UI (rollback, weight rebalancing)~~ ✅ `PromptVersionsPage.tsx` fully implemented with `promptVersioningApi.ts` — listPromptVersions/rollbackPromptVersion/updatePromptWeights; grouped by promptName table; active badge; View/Rollback/Weight action buttons; Rollback dialog with required reason field (Confirm disabled until filled), Cancel; Weight dialog with 0–1 numeric input; loading/error/empty states. Added `data-testid` attributes to root, loading-spinner, error-message, empty-state, version-row-{id}, rollback-dialog, weight-dialog. 10 tests covering loading, error, empty, render, active badge, rollback flow, disabled state, cancel, weight flow, rollback-hidden-for-active — all PASS. | M | MT |
| 68 | yappc | F-Y023 | ~~Document `SemanticCacheService` TTL/threshold; expose hit-ratio metric~~ ✅ Expanded class Javadoc to document default similarity threshold (0.95), TTL (24 h), max cache size (10 000), embedding dimension (1536), and thread-safety caveats. Added `hitRatio()` convenience method returning `hits/(hits+misses)` in [0.0,1.0] with full Javadoc. Added `SemanticCacheServiceTest` (10 tests) via `EventloopTestBase` covering `hitRatio()`, TTL expiry, similarity threshold, and `getStats()` consistency — all PASS. | S | ST |
| 63 | yappc | F-Y024 | ~~A/B testing operator UI (register variants, view results, promote winner)~~ ✅ `ABTestingDashboardPage.tsx` fully implemented with `listExperiments`, `createExperiment`, `promoteWinner`, `pauseExperiment` mutations via TanStack Query; view/promote/create dialogs; `ABTestingDashboardPage.test.tsx` covers all interactions. | M | MT |
| 64 | yappc | F-Y025 | ~~ESLint rule forbidding `@yappc/*` imports in `web/src`~~ ✅ Rule already existed in `governanceOverrides` but with incorrect path glob `products/yappc/frontend/web/src/**` (relative to where eslint runs, should be `web/src/**`). Fixed the glob. Also added `web/src/**/*.{ts,tsx}` to `lint` and `lint:fix` scripts in `package.json` — the lint script was not covering `web/src` at all. Rule stays `warn` (not `error`) because `@yappc/*` imports still exist in web/src pending migration. | S | ST |
| 65 | yappc | F-Y038 | ~~ArchUnit boundary tests: `agents` ⊄ `scaffold`/`refactorer`; `scaffold` ⊄ `agents`~~ ✅ Added `AgentsScaffoldRefactorerBoundaries` `@Nested` class to `YappcDependencyGovernanceTest` with 3 tests: `agentsMustNotDependOnScaffold`, `agentsMustNotDependOnRefactorer` (imports both yappc+refactorer packages), `scaffoldMustNotDependOnAgents`. All use `allowEmptyShould(true)`. `compileTestJava` BUILD SUCCESSFUL. | M | ST |
| 66 | yappc | F-Y041 | ~~Canonicalise GraphQL ↔ REST surface; document canonical surface per concept~~ ✅ `API_SURFACE_CANONICALIZATION.md` complete: domain→transport mapping tables, overlap elimination rules, enforcement checklist, and residual overlaps with action items. | M | MT |
| 67 | yappc | F-Y048 | ~~Require `Idempotency-Key` on approve/reject; persist replay window~~ ✅ Added `Idempotency-Key` header enforcement to `ApprovalHttpHandlers.decide()`: returns HTTP 422 when header absent. Added `IdempotencyStore` interface + `InMemoryIdempotencyStore` inner class (TTL-based, `ConcurrentHashMap<String,Entry>`, 24-hour default replay window, automatic expiry on `get()`). Scoped idempotency key as `{tenantId}:{requestId}:{action}:{idempotencyKey}` to prevent cross-tenant replay. Replayed responses include `X-Idempotent-Replayed: true` header. Added secondary `ApprovalHttpHandlers(service, mapper, idempotencyStore)` constructor for testability. BUILD SUCCESSFUL. | M | MT |
| 68 | yappc | F-Y053 | ~~Define mobile / IDE library scope or delete~~ ✅ `MOBILE_SKETCH_IDE_SCOPE.md` already exists in `products/yappc/frontend/libs/` and is fully authoritative: (1) `mobile/` classified as web-build Capacitor shim only — no published package, no product imports, lifecycle tied to native app workspace creation; (2) `@yappc/ide` v0.1.0 classified as YAPPC-only deprecated package, sunset 2026-06-06, migration target `@ghatana/code-editor` / `@ghatana/canvas/tools`, includes 4-step migration plan; (3) Sketch adapter declared non-existent, future work belongs in `platform/typescript/ds-generator`. Decision table and references included. No further action required. | S | ST |
| 69 | yappc | F-Y054 | ~~Delete root-level test scaffolding (`simple.test.ts`, etc.)~~ ✅ Found 3 test theatre files in `products/yappc/tests/integration/`: `extendedFlows.test.ts` (zero production imports), `endToEndFlows.test.ts` (testSetup mock stubs only), `testSetup.ts` (all `(data: any) => Promise<any>` mock interfaces). All deleted per Section 29 anti-theatre rule. | S | ST |

---

## P3 — Low Priority / Technical Debt

| # | Product | ID | Title | Complexity | Term |
|---|---|---|---|---|---|
| 70 | audio-video | AV-L1 | ~~Export load-test metrics in JSON/CSV instead of `console.log`~~ ✅ `PerformanceCollector.exportResults(outputDir)` in `load-test.spec.ts` writes JSON and CSV files to the output directory. | S | LT |
| 71 | audio-video | AV-L2 | ~~Finalize Testcontainers Docker Compose orchestration in `integration-tests`~~ ✅ `docker-compose.integration.yml` + `AudioVideoContainerBase.java` + `FullWorkflowIntegrationTest.java` exist in `audio-video/integration-tests/`. | M | LT |
| 72 | audio-video | AV-L3 | ~~Add smoke tests for `AudioVideoHealthService` endpoints~~ ✅ `AudioVideoHealthServiceSmokeIT.java` exists in `audio-video-observability/` with in-process gRPC server smoke tests. | S | LT |
| 73 | data-cloud | DC-L1 | ~~Implement `QueryOptimizer` real predicate/column/limit pushdown (`jsqlparser` based); benchmark~~ ✅ Done as part of DC-B3: `QueryOptimizer` uses jsqlparser for real limit pushdown; 6 tests covering boundary and normal cases. | L | LT |
| 74 | data-cloud | DC-L2 | ~~Add explicit semantic vector ID mapping tests in `platform-api/memory`~~ ✅ Refactored `SemanticSearchService` inner class in `MemorySemanticSearchTest` to use a `LinkedHashMap` keyed by ID; added `findById(id)` + `remove(id)` operations; added 7 new ID-mapping tests (`indexedItemIsRetrievableById`, `unknownIdReturnsEmpty`, `searchResultIdsMatchIndexedItems`, `removedItemDoesNotAppearInSearch`, `removedItemNotFoundById`, `removeIsScoped`, `duplicateIdOverwritesPreviousEmbedding`). | M | LT |
| 75 | data-cloud | DC-L3 | ~~Document learning status state machine (draft / active / retired) with examples~~ ✅ `LEARNING_STATUS_STATE_MACHINE.md` exists in `docs-generated/05-usage-manuals-and-api-docs/` with full state transition diagram and examples. | S | LT |
| 76 | data-cloud | DC-L4 | ~~Consolidate UI documentation across `ui/` and `libs/ui-components/` (shared `ARCHITECTURE.md`)~~ ✅ `ui/ARCHITECTURE.md` exists in `products/data-cloud/ui/` covering component hierarchy, design patterns, and integration contracts. | S | LT |
| 77 | aep | AEP-L1 | ~~Add audit/compliance test coverage in `aep-compliance`~~ ✅ Comprehensive coverage exists: `ComplianceTest.java` (129 lines — retention, compliance service orchestration), `AepComplianceServiceIntegrationTest.java` (65 lines), `DataRetentionAutomationServiceTest.java` (263 lines — automation lifecycle), `PostgresRetentionPolicyEnforcerTest.java` (DB enforcement); covers consent, purpose limitation, retention, and compliance service. | M | LT |

---

## Roll-up Summary

| Priority | Count | Audio-Video | Data-Cloud | AEP | YAPPC |
|---|---|---|---|---|---|
| P0 | 18 | 3 | 4 | 3 | 8 |
| P1 | 23 | 3 | 4 | 4 | 12 |
| P2 | 28 | 8 | 4 | 4 | 12 |
| P3 | 8 | 3 | 4 | 1 | 0 |
| **Total** | **77** | **17** | **16** | **12** | **32** |

| Term | Items |
|---|---|
| ST (1–2 weeks) | 38 |
| MT (1–3 months) | 30 |
| LT (3+ months) | 9 |

| Complexity | Items |
|---|---|
| S | 27 |
| M | 28 |
| L | 18 |
| XL | 4 |
