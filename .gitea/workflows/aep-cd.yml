# Ghatana AEP (Agentic Event Processor) — Continuous Deployment
#
# Deploys the AEP product to a target Kubernetes cluster after a
# successful CI run on the main branch or a release branch.
#
# Environments:
#   staging     — auto-deployed on every push to main
#   production  — requires manual approval gate before deploy
#
# Prerequisites (configured in Gitea → Settings → Secrets):
#   KUBECONFIG_STAGING    — base64-encoded kubeconfig for the staging cluster
#   KUBECONFIG_PRODUCTION — base64-encoded kubeconfig for the production cluster
#   GITEA_TOKEN           — registry pull secret token
#   SLACK_WEBHOOK_URL     — optional Slack notification

name: aep-cd

on:
  workflow_run:
    workflows: ["aep-ci"]
    types:
      - completed
    branches:
      - main
      - "release/**"
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production
      image_tag:
        description: "Image tag to deploy (default: latest SHA)"
        required: false
        default: ""

env:
  REGISTRY: ${{ gitea.server_url | replace('https://', '') | replace('http://', '') }}
  IMAGE_NAME: ${{ gitea.repository }}/aep
  K8S_NAMESPACE: aep

jobs:
  # ---------------------------------------------------------------------------
  # Guard: only proceed when CI passed
  # ---------------------------------------------------------------------------
  gate:
    name: CI Gate Check
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    outputs:
      proceed: ${{ steps.check.outputs.proceed }}
      image_tag: ${{ steps.resolve_tag.outputs.tag }}

    steps:
      - name: Gate check
        id: check
        run: echo "proceed=true" >> "$GITHUB_OUTPUT"

      - name: Resolve image tag
        id: resolve_tag
        run: |
          if [[ -n "${{ inputs.image_tag }}" ]]; then
            echo "tag=${{ inputs.image_tag }}" >> "$GITHUB_OUTPUT"
          else
            SHA=$(echo "${{ gitea.sha }}" | cut -c1-7)
            echo "tag=sha-${SHA}" >> "$GITHUB_OUTPUT"
          fi

  # ---------------------------------------------------------------------------
  # Deploy to Staging
  # ---------------------------------------------------------------------------
  deploy-staging:
    name: Deploy → Staging
    runs-on: ubuntu-latest
    needs: gate
    if: |
      needs.gate.outputs.proceed == 'true' &&
      (gitea.event_name == 'workflow_dispatch' && inputs.environment == 'staging' ||
       gitea.event_name == 'workflow_run')
    environment:
      name: staging
      url: https://api.aep.staging.ghatana.internal

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure kubectl (staging)
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_STAGING }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Verify cluster connection
        run: kubectl cluster-info --request-timeout=10s

      - name: Update image tag in kustomization overlay
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          TAG="${{ needs.gate.outputs.image_tag }}"
          # Patch the image tag in-place using kustomize edit
          cd products/aep/k8s
          kustomize edit set image aep-service-manager="${IMAGE}:${TAG}" || true

      - name: Apply Kubernetes manifests (kustomize)
        run: |
          kubectl apply -k products/aep/k8s/ \
            --namespace=${{ env.K8S_NAMESPACE }} \
            --server-side \
            --force-conflicts

      - name: Update deployment image directly (fallback)
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.gate.outputs.image_tag }}"
          kubectl set image deployment/aep-service-manager \
            aep-service-manager="${IMAGE}" \
            --namespace=${{ env.K8S_NAMESPACE }} || true

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/aep-service-manager \
            --namespace=${{ env.K8S_NAMESPACE }} \
            --timeout=300s

      - name: Smoke test (HTTP health check)
        run: |
          sleep 15
          CLUSTER_IP=$(kubectl get svc aep \
            -n ${{ env.K8S_NAMESPACE }} \
            -o jsonpath='{.spec.clusterIP}' 2>/dev/null || echo "")
          if [[ -n "${CLUSTER_IP}" ]]; then
            curl --fail --silent --max-time 10 \
              "http://${CLUSTER_IP}:8090/health" || \
            echo "Health check endpoint unreachable from runner — check cluster networking."
          fi

      - name: Notify Slack on success
        if: success() && env.SLACK_WEBHOOK_URL != ''
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -s -X POST "${SLACK_WEBHOOK_URL}" \
            -H 'Content-type: application/json' \
            --data '{
              "text": "✅ *aep* deployed to *staging* — `${{ needs.gate.outputs.image_tag }}`",
              "channel": "#aep-deploys"
            }'

      - name: Notify Slack on failure
        if: failure() && env.SLACK_WEBHOOK_URL != ''
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -s -X POST "${SLACK_WEBHOOK_URL}" \
            -H 'Content-type: application/json' \
            --data '{
              "text": "❌ *aep* staging deploy FAILED — `${{ needs.gate.outputs.image_tag }}`",
              "channel": "#aep-oncall"
            }'

  # ---------------------------------------------------------------------------
  # Deploy to Production (requires approval via Gitea environment protection)
  # ---------------------------------------------------------------------------
  deploy-production:
    name: Deploy → Production
    runs-on: ubuntu-latest
    needs: [gate, deploy-staging]
    if: |
      needs.gate.outputs.proceed == 'true' &&
      (startsWith(gitea.ref, 'refs/heads/release/') ||
       (gitea.event_name == 'workflow_dispatch' && inputs.environment == 'production'))
    environment:
      name: production
      url: https://api.aep.ghatana.io

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure kubectl (production)
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_PRODUCTION }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Verify cluster connection
        run: kubectl cluster-info --request-timeout=10s

      - name: Apply Kubernetes manifests (kustomize)
        run: |
          kubectl apply -k products/aep/k8s/ \
            --namespace=${{ env.K8S_NAMESPACE }} \
            --server-side \
            --force-conflicts

      - name: Update production image tag
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.gate.outputs.image_tag }}"
          kubectl set image deployment/aep-service-manager \
            aep-service-manager="${IMAGE}" \
            --namespace=${{ env.K8S_NAMESPACE }}

      - name: Wait for production rollout
        run: |
          kubectl rollout status deployment/aep-service-manager \
            --namespace=${{ env.K8S_NAMESPACE }} \
            --timeout=600s

      - name: Tag Git commit as released
        run: |
          TAG="release/aep-${{ needs.gate.outputs.image_tag }}"
          git config user.name  "Gitea CI"
          git config user.email "ci@ghatana.internal"
          git tag -a "${TAG}" -m "AEP production release ${TAG}"
          git push origin "${TAG}" || true

      - name: Notify Slack on success
        if: success() && env.SLACK_WEBHOOK_URL != ''
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -s -X POST "${SLACK_WEBHOOK_URL}" \
            -H 'Content-type: application/json' \
            --data '{
              "text": "🚀 *aep* deployed to *production* — `${{ needs.gate.outputs.image_tag }}`",
              "channel": "#aep-deploys"
            }'

      - name: Notify Slack on failure
        if: failure() && env.SLACK_WEBHOOK_URL != ''
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -s -X POST "${SLACK_WEBHOOK_URL}" \
            -H 'Content-type: application/json' \
            --data '{
              "text": "🔥 *aep* PRODUCTION deploy FAILED — `${{ needs.gate.outputs.image_tag }}`\n<!channel> immediate attention required!",
              "channel": "#aep-oncall"
            }'
