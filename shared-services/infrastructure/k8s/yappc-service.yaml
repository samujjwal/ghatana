apiVersion: v1
kind: Service
metadata:
  name: yappc
  namespace: ghatana
  labels:
    app: yappc
    component: devsecops-platform
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: metrics
    protocol: TCP
  selector:
    app: yappc
