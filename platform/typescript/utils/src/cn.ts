/**
 * @deprecated Use `@ghatana/platform-utils` directly instead of this package.
 * This compatibility re-export will be removed in a future version.
 *
 * @example
 * ```ts
 * // Preferred: import from the canonical package
 * import { cn, type ClassValue } from '@ghatana/platform-utils';
 * ```
 */
export { cn, type ClassValue } from '@ghatana/platform-utils/cn';
