# Shared TypeScript Utils – Operations Guide

## 1. Overview

The Shared TypeScript Utils library is a pure library; operational impact is via apps and libs that depend on it.

## 2. Versioning

- Use semantic versioning.
- Coordinate upgrades with consuming projects to avoid regressions.

This guide is self-contained and documents operational considerations for the Shared TypeScript Utils library.
