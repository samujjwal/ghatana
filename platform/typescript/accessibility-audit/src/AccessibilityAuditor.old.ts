// Archived: this legacy auditor implementation has been moved to
// `libs/accessibility-audit/archive/AccessibilityAuditor.old.ts`.
//
// The legacy implementation is preserved for historical reference. The
// active implementation lives in the main source files. This stub prevents
// accidental compilation of the legacy code while keeping a clear pointer
// to the archived copy.

export {}; // noop export to keep TypeScript happy
