# Shared TypeScript Theme – Coding Guidelines

## 1. Scope

These guidelines apply to `libs/typescript/theme`.

## 2. Design Principles

- Build themes on top of shared tokens.
- Keep theme definitions declarative and framework-friendly.
- Avoid embedding app-specific logic; keep this library generic.

This document is self-contained and defines how to structure code in the Shared TypeScript Theme library.
