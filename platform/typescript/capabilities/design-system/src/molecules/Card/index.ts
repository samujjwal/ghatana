// Re-export all components from the main Card file
export * from '../Card';

// Re-export SurfaceProps as CardSurfaceProps for backward compatibility
export type { SurfaceProps as CardSurfaceProps } from '../../layout/Surface';

