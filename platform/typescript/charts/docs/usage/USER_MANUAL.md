# Shared TypeScript Charts – User Manual

## 1. Audience

This manual is for frontend engineers using `libs/typescript/charts`.

## 2. Basic Usage

1. Add the library as a dependency in your app.
2. Import shared chart components and pass typed data and configuration props.

This manual is self-contained and explains how to use the Shared TypeScript Charts library in typical workflows.
