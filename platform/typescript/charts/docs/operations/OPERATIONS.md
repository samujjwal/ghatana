# Shared TypeScript Charts – Operations Guide

## 1. Overview

The Shared TypeScript Charts library is a pure frontend library; operational impact is via apps that consume it.

## 2. Versioning

- Use semantic versioning.
- Coordinate upgrades with app teams, especially when chart behavior or props change.

This guide is self-contained and documents operational considerations for the Shared TypeScript Charts library.
