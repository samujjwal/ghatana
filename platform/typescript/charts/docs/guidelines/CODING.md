# Shared TypeScript Charts – Coding Guidelines

## 1. Scope

These guidelines apply to `libs/typescript/charts`.

## 2. Design Principles

- Keep chart components configurable and reusable.
- Model props with typed data structures aligned with domain/API models.
- Integrate styling with shared theme and tokens.

This document is self-contained and defines how to structure code in the Shared TypeScript Charts library.
