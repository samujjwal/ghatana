{
  "kernelCapabilities": {
    "approval-workflow": {
      "description": "Kernel-managed approval workflow orchestration."
    },
    "audit-trail": {
      "description": "Kernel audit trail and immutable evidence emission."
    },
    "boundary-policy-evaluation": {
      "description": "Kernel policy boundary evaluation for product actions."
    },
    "consent-enforcement": {
      "description": "Kernel consent enforcement and delegation checks."
    },
    "human-approval": {
      "description": "Kernel human-in-the-loop approval capability."
    },
    "observability": {
      "description": "Kernel observability, trace, metric, and log propagation."
    },
    "tenant-context": {
      "description": "Kernel tenant context propagation and validation."
    }
  },
  "plugins": {
    "plugin-audit-trail": {
      "description": "Tamper-evident audit event ledger."
    },
    "plugin-compliance": {
      "description": "Product compliance rule evaluation."
    },
    "plugin-consent": {
      "description": "Consent grant, revocation, and purpose-bound access lifecycle."
    },
    "plugin-fraud-detection": {
      "description": "Fraud signal evaluation and gating."
    },
    "plugin-human-approval": {
      "description": "Human approval gates for sensitive workflows."
    },
    "plugin-ledger": {
      "description": "Double-entry ledger integrity."
    },
    "plugin-risk-management": {
      "description": "Risk scoring and product action gating."
    }
  },
  "domainPacks": {
    "kernel-canonical": {
      "description": "Canonical Kernel domain packs - products declare their own domain packs in manifests"
    }
  },
  "policyActions": {
    "canonical": ["read", "write", "delete", "export", "download"],
    "note": "Product-specific policy actions are declared in product manifests with product prefix (e.g., finance:settle, digital-marketing:launch)"
  },
  "policyResources": {
    "canonical": [],
    "note": "Product-specific policy resources are declared in product manifests with product prefix (e.g., finance:transactions, phr:subject-records)"
  }
}
