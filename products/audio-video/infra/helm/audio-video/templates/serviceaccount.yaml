{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "audio-video.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "audio-video.labels" . | nindent 4 }}
{{- end }}