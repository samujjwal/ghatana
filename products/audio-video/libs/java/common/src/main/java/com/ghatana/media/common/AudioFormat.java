package com.ghatana.media.common;

/**
 * Audio format types.
 *
 * @doc.type enum
 * @doc.purpose Audio container and encoding format identifiers
 * @doc.layer common
 * @doc.pattern ValueObject
 */
public enum AudioFormat {
    PCM,
    WAV,
    MP3,
    FLAC,
    OGG,
    AAC
}
