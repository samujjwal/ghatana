{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Default capability for the main audio-video desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}