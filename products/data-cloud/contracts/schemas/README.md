# Data Cloud Schemas

This folder is reserved for product-owned JSON Schema, AsyncAPI fragments, event schemas, and shared contract schemas that are not OpenAPI documents.

OpenAPI contracts live in `../openapi/`.
