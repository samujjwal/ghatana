package com.ghatana.core.event.query;


/**
 * @doc.type enum
 * @doc.purpose Provides aggregation function functionality.
 * @doc.layer product
 * @doc.pattern Component
 */
public enum AggregationFunction {
    COUNT,
    SUM,
    AVG,
    MIN,
    MAX,
    DISTINCT_COUNT,
    FIRST,
    LAST
}
