plugins { id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
} rootProject.name = '{{projectName}}'

{{moduleIncludes}}

// Configure dependency resolution strategy dependencyResolutionManagement {
repositories { mavenCentral() gradlePluginPortal() } // Use version catalog for
dependency management versionCatalogs { libs {
from(files("gradle/libs.versions.toml")) } } }

{{monorepoSettings}}