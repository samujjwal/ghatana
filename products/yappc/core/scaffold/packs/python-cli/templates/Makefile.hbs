.PHONY: install run test lint format clean

install:
{{#if buildTool}}
{{#eq buildTool "uv"}}
uv sync
{{/eq}}
{{#eq buildTool "poetry"}}
poetry install
{{/eq}}
{{else}}
uv sync
{{/if}}

run:
{{#if buildTool}}
{{#eq buildTool "uv"}}
uv run {{snakeCase projectName}}
{{/eq}}
{{#eq buildTool "poetry"}}
poetry run {{snakeCase projectName}}
{{/eq}}
{{else}}
uv run {{snakeCase projectName}}
{{/if}}

test:
pytest

lint:
ruff check .
mypy .

format:
ruff format .

clean:
find . -type d -name __pycache__ -exec rm -rf {} +
find . -type f -name "*.pyc" -delete
rm -rf .pytest_cache .mypy_cache .ruff_cache
rm -rf dist build *.egg-info