-- Initial schema migration for {{projectName}}
-- Generated by YAPPC scaffold

-- Enable UUID extension (PostgreSQL)
{{#if (eq databaseType "postgresql")}}
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
{{/if}}

-- Example table structure
CREATE TABLE IF NOT EXISTS example_entity (
id {{#if (eq databaseType "postgresql")}}UUID PRIMARY KEY DEFAULT uuid_generate_v4(){{else if (eq databaseType
"mysql")}}CHAR(36) PRIMARY KEY{{else}}TEXT PRIMARY KEY{{/if}},
name VARCHAR(255) NOT NULL,
description TEXT,
created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
version BIGINT NOT NULL DEFAULT 0
);

-- Create index for common queries
CREATE INDEX idx_example_entity_name ON example_entity(name);
CREATE INDEX idx_example_entity_created_at ON example_entity(created_at);