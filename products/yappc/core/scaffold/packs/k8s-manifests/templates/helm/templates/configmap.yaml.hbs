apiVersion: v1
kind: ConfigMap
metadata:
name: {{ include "{{kebabCase projectName}}.fullname" . }}-config
labels:
app: {{ include "{{kebabCase projectName}}.fullname" . }}
data:
APP_ENV: "production"