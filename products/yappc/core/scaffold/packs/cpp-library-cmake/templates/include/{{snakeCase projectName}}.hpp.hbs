#pragma once

#include <string>

    namespace {{snakeCase projectName}} {

    class {{pascalCase projectName}} {
    public:
    {{pascalCase projectName}}();
    ~{{pascalCase projectName}}();

    std::string get_version() const;
    };

    } // namespace {{snakeCase projectName}}