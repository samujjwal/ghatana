// Archived: legacy UX system entrypoint moved to
// Archived: legacy UX system entrypoint moved to
// `libs/canvas/archive/index-old.ts`.
//
// The archive preserves legacy usage examples and exports for historical
// reference. The current, supported UX entrypoints are in `src/ux/*`.

export {};