# Shared Product Types

Shared TypeScript contracts used across YAPPC web routes, components, and services.

## Scope
- Product-level types reused across multiple frontend layers.
- Shared lifecycle, artifact, and workflow contracts.
- Stable interfaces for cross-module integration inside the web app.

## Key Areas
- Lifecycle and artifact models.
- Cross-feature shared contracts.
- Supporting type tests where present.