/**
 * @fileoverview Contracts Module - Exports
 *
 * Central export point for all extension contracts:
 * - Message contracts (sink config, commands, processes)
 * - Configuration contracts (bootstrap, runtime, extension)
 * - Message handlers and routing
 */

// Message contracts
export * from './messages';

// Configuration contracts
export * from './config';

// Message handlers
export * from './handlers';
