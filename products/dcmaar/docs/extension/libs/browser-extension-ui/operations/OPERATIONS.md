# DCMaar – Browser Extension UI – Operations Guide

## 1. Overview

`@dcmaar/browser-extension-ui` is a library; operational impact is through its adoption in extension bundles.

## 2. Versioning

- Coordinate component changes with extension apps to prevent visual regressions.

This guide is self-contained and documents operational considerations for `@dcmaar/browser-extension-ui`.
