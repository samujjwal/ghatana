# DCMaar – Shared UI Tailwind – Operations Guide

## 1. Overview

`@dcmaar/shared-ui-tailwind` is a library; operational impact is via shared usage across DCMAAR UIs.

## 2. Versioning

- Coordinate component and style changes with all consuming apps to avoid regressions.

This guide is self-contained and documents operational considerations for `@dcmaar/shared-ui-tailwind`.
