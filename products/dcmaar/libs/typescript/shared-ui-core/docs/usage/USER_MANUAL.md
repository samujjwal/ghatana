# DCMaar – Shared UI Core – User Manual

## 1. Audience

This manual is for UI engineers using `@dcmaar/shared-ui-core` in DCMaar UIs.

## 2. Basic Usage

1. Add `@dcmaar/shared-ui-core` as a dependency.
2. Import tokens, types, hooks, and utilities into higher-level UI components.

## 3. Best Practices

- Centralize visual and behavioral primitives here rather than duplicating.

This manual is self-contained and explains how to use `@dcmaar/shared-ui-core` in typical flows.
