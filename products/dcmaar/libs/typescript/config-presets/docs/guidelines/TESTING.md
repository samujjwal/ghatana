# DCMaar – Config Presets – Testing Guidelines

## 1. Goals

- Ensure presets are valid against schemas and align with consumer expectations.

## 2. Tests

- Add tests that parse presets through Zod schemas and assert expected values.

This document is self-contained and explains how to test `@dcmaar/config-presets`.
