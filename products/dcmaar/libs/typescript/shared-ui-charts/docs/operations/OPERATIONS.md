# DCMaar – Shared UI Charts – Operations Guide

## 1. Overview

`@dcmaar/shared-ui-charts` is a library; operational impact is via usage across dashboards and apps.

## 2. Versioning

- Coordinate chart changes with consumers to avoid visual regressions.

This guide is self-contained and documents operational considerations for `@dcmaar/shared-ui-charts`.
