# DCMaar – Agent UI – Operations Guide

## 1. Overview

`@dcmaar/agent-ui` is a library; operational impact is via adoption across agent-facing UIs.

## 2. Versioning

- Coordinate component and style changes with consuming apps to avoid regressions.

This guide is self-contained and documents operational considerations for `@dcmaar/agent-ui`.
