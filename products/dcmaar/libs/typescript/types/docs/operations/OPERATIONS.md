# DCMaar – Types – Operations Guide

## 1. Overview

`@dcmaar/types` is a foundational library; operational impact is via versioning and dependency management.

## 2. Versioning

- Coordinate type changes with all dependent packages.
- Use semantic versioning to indicate breaking vs. non-breaking changes.

This guide is self-contained and documents operational considerations for `@dcmaar/types`.
