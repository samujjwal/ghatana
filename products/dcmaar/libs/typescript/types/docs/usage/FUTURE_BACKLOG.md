# DCMaar – Types – Future Backlog

## 1. Short-Term

- Consolidate duplicated type definitions from other packages into `@dcmaar/types`.

## 2. Medium-Term

- Introduce more granular submodules for clearer type organization.

This backlog is self-contained and outlines potential evolution paths for `@dcmaar/types`.
