/**
 * DCMAAR UI types sub-module.
 *
 * Provides DCMAAR-specific UI primitives (ConnectionStatus, TimeRange, formatUptime)
 * that were previously in `@dcmaar/ui/core`.
 *
 * @doc.type module
 * @doc.purpose DCMAAR-specific UI type and utility exports
 * @doc.layer product
 * @doc.pattern Barrel Export
 */
export * from './core';
