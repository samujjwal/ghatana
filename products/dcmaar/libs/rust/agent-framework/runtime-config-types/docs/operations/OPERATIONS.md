# DCMaar Runtime Config Types – Operations Guide

## 1. Overview

`runtime-config-types` is a library. Operational concerns relate to schema and codegen management.

## 2. Schema Changes

- Coordinate schema changes with all consumers.
- Regenerate types and run tests for dependent crates.

This guide is self-contained and documents operational considerations for `runtime-config-types`.
