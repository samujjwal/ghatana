# DCMaar Runtime Config Types – Testing Guidelines

## 1. Goals

- Ensure configuration types correctly reflect the underlying schema and support round-tripping.

## 2. Tests

- Add tests for serialization/deserialization of key configuration examples.

This document is self-contained and explains how to test `runtime-config-types`.
