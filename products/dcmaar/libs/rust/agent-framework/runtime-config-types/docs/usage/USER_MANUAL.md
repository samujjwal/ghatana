# DCMaar Runtime Config Types – User Manual

## 1. Audience

This manual is for Rust engineers consuming `runtime-config-types`.

## 2. Basic Usage

1. Add `runtime-config-types` as a dependency.
2. Use its types to represent and validate runtime configuration.

This manual is self-contained and explains how to use `runtime-config-types` in typical flows.
