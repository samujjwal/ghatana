# DCMaar Runtime Config Types – Known Issues & Troubleshooting

## 1. Known Issues

- Schema changes that are not properly coordinated can lead to configuration errors.

## 2. Troubleshooting

- When configuration fails to parse, verify that the runtime config schema and generated types are in sync.

This document is self-contained and lists common issues and mitigations for `runtime-config-types`.
