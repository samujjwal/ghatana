# DMOS Kernel/Platform Enhancement TODO Backlog

**Repository:** `samujjwal/ghatana`  
**Target head:** `1577a8559f3ae6a973452465a2cedf8d52c0a0ef`  
**Rule:** Only product-agnostic, reusable tasks are included. DMOS-specific campaign, ad, content, marketing, Google Ads semantics, product prompts, policy packs, UI routes, and DTOs stay in DMOS.

| Priority | Capability | Evidence From DMOS | Why Product-Agnostic | Proposed Location | Public Contract | Acceptance Criteria | Tests Required |
|---|---|---|---|---|---|---|---|
| P1 | Production composition verifier | Static tests missed runtime missing repo wiring. | Any product with a composition root needs fail-closed graph validation. | `platform/java/runtime` + `platform/java/testing` | `ProductionGraphVerifier` with required ports, forbidden adapters, no-op detector. | Missing durable port or in-memory/no-op adapter fails startup/CI. | Shared verifier tests; DMOS sample. |
| P1 | Generic connector runtime | DMOS connector command path needs durable config, credentials, health, retry, outbox. | Connectors are used by Data Cloud, Finance, PHR, DMOS. | `platform/java/integration` or `platform-plugins/plugin-connector` | `ConnectorConfigRepository`, `CredentialVaultPort`, `ConnectorHealthPort`, `ConnectorCommandOutbox`, `RetryPolicy`. | Products define domain mapping only; platform handles lifecycle. | Contract/failure/retry tests. |
| P1 | Token vault / OAuth credential abstraction | DMOS token storage is product-specific and not wired. | OAuth/token storage is common. | `platform/java/security` or `shared-services/token-vault` | `TokenVaultPort` with store/load/revoke/rotate/list. | Encrypted, tenant-scoped, auditable tokens reusable by products. | Encryption, key rotation, revocation, tenant tests. |
| P1 | AI action audit envelope | Strategy provenance/fallback handling needs standard audit records. | All AI products need prompt/output/status/failure trace. | `platform/java/ai-integration` + audit plugin | `AiActionAuditEnvelope` with actor, tenant, workspace, model, prompt hash, input/output refs, status, confidence, approval id. | DMOS and other products log AI actions without domain leakage. | Envelope serialization and product integration tests. |
| P1 | Standard API error envelope middleware | Approval and campaign servlets return different shapes. | API consistency is platform-level. | `platform/java/http` | `ApiErrorEnvelope`, `ErrorMapper`, `CorrelationIdProvider`. | Java products can return consistent `{error,message,status,correlationId,details}`. | Golden contract tests. |
| P1 | Policy pack validation harness | DMOS default-deny validation is incomplete. | Policy validation is generic. | `platform/java/governance` or `platform/java/policy-as-code` | `PolicyPackValidator` with required rule ids, default-deny assertion, namespace hygiene. | Missing/malformed default-deny pack fails CI/startup. | Valid/invalid pack tests. |
| P1 | PII-safe observability redaction policy | DMOS telemetry may emit sensitive identifiers. | All products need safe logs/traces. | `platform/java/observability` | `TelemetryRedactionPolicy`, `SensitiveValueHasher`, safe attribute API. | Raw PII/secrets/idempotency keys cannot be emitted accidentally. | Span/log redaction tests. |
| P1 | Multi-tenant repository/API test harness | DMOS needs reusable cross-tenant leakage tests. | Multi-tenancy is common. | `platform/java/testing` | `TenantIsolationTestHarness` for repo/API operations. | Products can define operations and get negative cross-tenant tests. | Harness self-tests + DMOS sample. |
| P1 | Idempotent command/outbox runtime | DMOS external launches need durable idempotency and rollback. | External side effects are common. | `platform/java/workflow` or `platform/java/runtime` | `CommandOutboxPort`, `CommandDispatcher`, `RetryPolicy`, `RollbackHandler`. | Product handlers are durable, retryable, observable. | Concurrency/duplicate/retry/rollback tests. |
| P2 | Route/capability contract generator | UI and backend capability mappings can drift. | Product route auth is reusable. | `platform/typescript/product-shell` + `platform/java/security` | Manifest schema and TS/Java codegen. | UI/backend derive from shared capability source. | Codegen drift tests. |
| P2 | Health/readiness model | DMOS uses no-op health indicator. | All products need standard dependency checks. | `platform/java/runtime` or `platform/java/observability` | `HealthIndicator`, `ReadinessProbe`, `DependencyHealth`, `HealthServlet`. | Products expose dependency-aware health/readiness. | Failed dependency tests. |
| P2 | Feature flag manifest/codegen utility | DMOS has feature flag manifest/codegen pattern. | Feature flags are generic boilerplate. | `platform/java/config` + TS utils | Feature flag manifest schema and TS/Java generation. | No per-product duplicated flag generator. | Codegen/CI drift tests. |
