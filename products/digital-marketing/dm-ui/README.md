# Archived DMOS UI Surface

`dm-ui` is an archived, non-built UI surface retained only for historical reference.

The production DMOS frontend is `products/digital-marketing/ui`. Do not add new
features, production checks, or release-gate coverage to this directory. Any useful
component or test should be moved into `ui` before it is considered part of the
product.
