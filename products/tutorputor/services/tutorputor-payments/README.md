# Consolidated into tutorputor-platform

This service directory is retained only as a compatibility placeholder.

Active implementation lives in:

- ../tutorputor-platform/src/modules/payments
- ../tutorputor-platform/src/modules/integration/billing

Do not add new runtime code here unless the architecture is explicitly split again.
