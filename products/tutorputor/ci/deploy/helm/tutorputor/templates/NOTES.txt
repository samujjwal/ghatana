TutorPutor Platform has been deployed!

1. Get the application URL:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  https://{{ $host.host }}
{{- end }}
{{- else }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "{{ include "tutorputor.selectorLabels" . }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 3000:3000
  Open http://127.0.0.1:3000
{{- end }}

2. Platform shared services status:
{{- if .Values.platformServices.authGatewayUrl }}
  ✅ Auth Gateway: {{ .Values.platformServices.authGatewayUrl }}
{{- else }}
  ⚠️  Auth Gateway: DISABLED (AUTH_GATEWAY_URL not set — local JWT auth only)
{{- end }}
{{- if .Values.platformServices.aiRegistryUrl }}
  ✅ AI Registry:  {{ .Values.platformServices.aiRegistryUrl }}
{{- else }}
  ⚠️  AI Registry:  DISABLED (AI_REGISTRY_URL not set)
{{- end }}
{{- if .Values.platformServices.featureStoreUrl }}
  ✅ Feature Store: {{ .Values.platformServices.featureStoreUrl }}
{{- else }}
  ⚠️  Feature Store: DISABLED (FEATURE_STORE_URL not set)
{{- end }}

3. Check pod health:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl logs -n {{ .Release.Namespace }} -l "{{ include "tutorputor.selectorLabels" . }}" --tail=50
