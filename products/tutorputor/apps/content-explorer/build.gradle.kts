/**
 * content-explorer — React/Vite frontend application.
 *
 * Build management is handled entirely by pnpm/Vite (not Gradle).
 * This file satisfies monorepo Gradle discovery only.
 *
 * Dev:   pnpm --filter @ghatana/content-explorer dev   (port 3205)
 * Build: pnpm --filter @ghatana/content-explorer build
 * Test:  pnpm --filter @ghatana/content-explorer test
 *
 * The app proxies /api → localhost:3200 (tutorputor-api-gateway).
 */
// no-op Gradle module
