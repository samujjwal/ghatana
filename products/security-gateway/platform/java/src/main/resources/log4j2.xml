<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n%throwable{full}</Property>
        <Property name="LOG_DIR">logs</Property>
        <Property name="ARCHIVE_PATTERN">${LOG_DIR}/%d{yyyy-MM}/security-%d{yyyy-MM-dd}-%i.log.gz</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- File Appender -->
        <RollingFile name="File" fileName="${LOG_DIR}/security.log" 
                     filePattern="${ARCHIVE_PATTERN}">
            <PatternLayout>
                <Pattern>${LOG_PATTERN}</Pattern>
            </PatternLayout>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30">
                <Delete basePath="${LOG_DIR}" maxDepth="2">
                    <IfFileName glob="security-*.log.gz" />
                    <IfLastModified age="30d" />
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

        <!-- Async Appender for better performance -->
        <Async name="AsyncFile" bufferSize="1024">
            <AppenderRef ref="File"/>
        </Async>

        <!-- Special appender for security events -->
        <RollingFile name="SecurityAudit" fileName="${LOG_DIR}/security-audit.log"
                    filePattern="${LOG_DIR}/%d{yyyy-MM}/security-audit-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout>
                <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} | %X{requestId} | %X{userId} | %X{clientIp} | %msg%n</Pattern>
            </PatternLayout>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="100 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Default logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncFile"/>
        </Root>

        <!-- Security-specific loggers -->
        <Logger name="com.ghatana.security" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Security audit logger -->
        <Logger name="com.ghatana.security.audit" level="info" additivity="false">
            <AppenderRef ref="SecurityAudit"/>
        </Logger>

        <!-- JWT and OAuth2 logging -->
        <Logger name="com.nimbusds" level="warn"/>
        <Logger name="io.jsonwebtoken" level="warn"/>
        
        <!-- Reduce noise from other libraries -->
        <Logger name="org.apache" level="warn"/>
        <Logger name="io.netty" level="warn"/>
    </Loggers>
</Configuration>
