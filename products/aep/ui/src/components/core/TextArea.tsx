/**
 * TextArea — re-export from shared design-system.
 *
 * @doc.type component
 * @doc.purpose Standardized textarea component via shared design system
 * @doc.layer frontend
 */
export { TextArea } from "@ghatana/design-system";
export type { TextAreaProps } from "@ghatana/design-system";
