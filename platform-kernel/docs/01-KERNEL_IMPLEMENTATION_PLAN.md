Below is the implementation-ready backlog for `samujjwal/ghatana` at `cf61a7ead6dd44c8a130bf02f8522e05c89a0e41`, focused on **feature completeness, correctness, E2E behavior, and production-grade quality**.

The active scope includes Data-Cloud, AEP/Action Plane, YAPPC, Audio-Video, Kernel bridges, and shared platform modules from the generated Gradle registry. 

---

# P0 — Production blockers

| ID        | Product         | Where                                                              | What to do                                                                                                                                                                                                                        | Tests / acceptance                                                                                                                     |
| --------- | --------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| AV-P0-001 | Audio-Video STT | `products/audio-video/modules/speech/stt-service/build.gradle.kts` | Fix `application.mainClass` and `runSttService.mainClass`. Current Gradle config points to `com.ghatana.stt.core.grpc.SttGrpcServer`, but actual class package is `com.ghatana.stt.grpc.SttGrpcServer`.                           | Add/verify `:products:audio-video:modules:speech:stt-service:installDist`, `distZip`, and `runSttService` resolve and start the class. |
| AV-P0-002 | Audio-Video TTS | `products/audio-video/modules/speech/tts-service/build.gradle.kts` | Fix `application.mainClass` and `runTtsService.mainClass`. Current Gradle config points to `com.ghatana.tts.core.grpc.TtsGrpcServer`, but actual class package is `com.ghatana.tts.grpc.TtsGrpcServer`.                           | Add/verify `:products:audio-video:modules:speech:tts-service:installDist`, `distZip`, and `runTtsService` resolve and start the class. |
| AV-P0-003 | Audio-Video     | `products/audio-video/modules/**/build.gradle.kts`                 | Add service-launch smoke tests for STT, TTS, Vision, and Multimodal so broken `mainClass` paths cannot recur. Vision and Multimodal already define launcher main classes and run tasks; include them in the same smoke pattern.   | New Gradle task or CI check starts each service in dry-run/smoke mode and fails on classpath/mainClass errors.                         |

---

# P1 — Major feature completeness and E2E gaps

| ID        | Product                       | Where                                                                                                    | What to do                                                                                                                                                                                                                                                                                                                                   | Tests / acceptance                                                                                                                 |
| --------- | ----------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| AV-P1-001 | Audio-Video common            | `products/audio-video/libs/common/src/main/java/**`, `products/audio-video/modules/*/*/src/main/java/**` | Create one shared request-context/security/audit interceptor for gRPC services. It must extract tenant, principal, roles, request/correlation ID, and apply auth/RBAC consistently before service logic runs. STT currently performs real transcription but the request path shown does not enforce tenant/security/audit.                   | Add negative tests for missing tenant, invalid auth, cross-tenant request, and insufficient role across STT/TTS/Vision/Multimodal. |
| AV-P1-002 | Audio-Video STT               | `products/audio-video/modules/speech/stt-service/src/main/java/com/ghatana/stt/grpc/SttGrpcService.java` | Add production-grade STT metadata persistence: request ID, tenant ID, model ID, language/options, confidence, processing time, input metadata hash, output metadata, and redacted transcript policy. STT currently returns text/confidence/model/processing time but does not show persistence/audit/event emission in the transcribe path.  | Integration test: gRPC transcribe → persistence record → audit event → metrics/traces with tenant/correlation IDs.                 |
| AV-P1-003 | Audio-Video STT               | `SttGrpcService.java`                                                                                    | Add STT audit events for success, validation failure, inference failure, timeout/cancellation, and degraded provider failure.                                                                                                                                                                                                                | Tests assert audit payload has tenant, principal, operation, sensitivity, model, success/failure, and redacted details.            |
| AV-P1-004 | Audio-Video STT               | `SttGrpcService.java`                                                                                    | Add explicit language handling and supported-format validation. Current path assumes PCM using request sample rate and defaults rather than validating format/language contract.                                                                                                                                                             | Tests for unsupported format, invalid sample rate, unsupported language, empty audio, too-large audio, and valid request.          |
| AV-P1-005 | Audio-Video STT               | `SttGrpcService.java`                                                                                    | Add bounded timeout/cancellation behavior for batch transcribe and streaming transcribe. Streaming currently has buffer backpressure and processor shutdown, but production timeout/cancellation policy needs to be explicit.                                                                                                                | Tests for client cancellation, server timeout, buffer exhaustion, session close, and no leaked processor/session.                  |
| AV-P1-006 | Audio-Video TTS               | `products/audio-video/modules/speech/tts-service/src/main/java/**`                                       | Add/verify TTS tenant/security/audit/persistence parity with STT. TTS module declares governance/audit/persistence dependencies but needs proven runtime use.                                                                                                                                                                                | E2E: synthesize text → validate voice/model → produce audio → persist metadata → audit event → metrics.                            |
| AV-P1-007 | Audio-Video Vision            | `products/audio-video/modules/vision/vision-service/src/main/java/**`                                    | Add/verify Vision tenant/security/audit/persistence parity. Vision module declares governance/audit/persistence dependencies and OpenCV runtime, but production feature paths must prove validation, extraction, metadata, and failure behavior.                                                                                             | E2E: image/video request → format validation → extraction result → confidence metadata → persistence/audit/metrics.                |
| AV-P1-008 | Audio-Video Multimodal        | `products/audio-video/modules/intelligence/multimodal-service/src/main/java/**`                          | Complete multimodal orchestration across STT, Vision, TTS where applicable. It depends on STT, Vision, persistence, agent-core, governance, and audit, but must prove composition and routing.                                                                                                                                               | E2E: audio+video+text input → STT/Vision routing → aggregated result → persistence/audit → degraded fallback.                      |
| AV-P1-009 | Audio-Video ↔ Data-Cloud      | `products/audio-video/modules/**`, `products/data-cloud/**`                                              | Add Data-Cloud event/evidence bridge for media processing results. Media processing should emit metadata/evidence events consumable by Data-Cloud and optionally AEP.                                                                                                                                                                        | E2E: media request → result persisted → Data-Cloud event emitted → query by tenant → no cross-tenant leak.                         |
| AV-P1-010 | Audio-Video Integration Tests | `products/audio-video/modules/integration-tests/src/test/java/**`                                        | Add real integration tests. The integration-test module exists and depends on STT/TTS/Vision/Multimodal/persistence/common, but the task must contain real service-level E2E tests, not only dependency wiring.                                                                                                                              | Tests named `*IT`/`*E2ETest` cover STT, TTS, Vision, Multimodal, and cross-service composition.                                    |

---

# Data-Cloud feature completeness tasks

| ID        | Product                | Where                                                                                                                     | What to do                                                                                                                                                                                                                                               | Tests / acceptance                                                                                                           |
| --------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| DC-P1-001 | Data-Cloud             | `products/data-cloud/delivery/launcher/src/main/java/com/ghatana/datacloud/launcher/http/handlers/EntityCrudHandler.java` | Make non-transactional entity write/batch paths explicitly local-only. Production-like profiles already require durable idempotency, transaction manager, audit service, and outbox, but fallback paths still exist when `transactionManager == null`.   | Production-profile test fails startup/request if transaction manager/outbox/audit/idempotency are absent.                    |
| DC-P1-002 | Data-Cloud             | `EntityCrudHandler.java`, launcher startup wiring                                                                         | Register `EntityCrudHandler.validateProductionRequirements()` through central startup/runtime profile validation so production deployments cannot forget to call it.                                                                                     | Test starts server in production-like profile with missing dependency and expects startup failure.                           |
| DC-P1-003 | Data-Cloud             | `EntityCrudHandler.java`                                                                                                  | Make batch delete all-or-nothing behavior production-safe for all destructive collections, not only hardcoded `patients`, `consents`, `audit`, `emergency_access`. Current critical collection list is hardcoded.                                        | Add config/registry-driven destructive collection policy and tests for custom collection classification.                     |
| DC-P1-004 | Data-Cloud             | `EntityCrudHandler.java`                                                                                                  | Add full E2E for dry-run → confirmation token → transactional delete → per-entity events → audit → outbox → response. Token signing/validation now includes count and is corrected.                                                                      | Test uses real/integration stores, verifies CDC envelopes and audit records, not mocks only.                                 |
| DC-P1-005 | Data-Cloud             | `EntityCrudHandler.java`                                                                                                  | Fix/verify per-entity delete event mapping for batch delete. Current transactional event loop indexes `existingEntities.get(i)` while iterating over `deletedIds`; if some deletes fail or list ordering diverges, wrong before-state can be attached.   | Test partial failures and assert each delete event contains the correct original entity snapshot.                            |
| DC-P1-006 | Data-Cloud             | `EntityCrudHandler.java`                                                                                                  | Ensure batch delete in production never logs and continues with in-memory outbox. One non-transactional path logs production in-memory outbox but does not fail request.                                                                                 | Production-profile test with `InMemoryEntityWriteOutboxProcessor` must fail, not warn-and-continue.                          |
| DC-P1-007 | Data-Cloud             | `EntityCrudHandler.java`, `DataCloudClient`, event store                                                                  | Add atomicity proof for entity save + event append + audit/outbox. Transactional save path exists, but E2E must prove rollback on event/audit/outbox failure.                                                                                            | Failure-injection tests: save fails, event append fails, audit/outbox fails; no partial state remains.                       |
| DC-P1-008 | Data-Cloud             | `EntityCrudHandler.handleUpsertCollectionMetadata`                                                                        | Add domain event and audit/outbox emission for collection metadata changes. Current path validates, merges, and saves `dc_collections`, but does not show domain event/audit emission in that handler.                                                   | Test metadata upsert emits `collection.metadata.updated` event and audit record.                                             |
| DC-P1-009 | Data-Cloud Event Plane | `products/data-cloud/planes/event/**`, `delivery/launcher/http/handlers/EventHandler.java`                                | Complete and prove append/read/tail/replay/checkpoint/DLQ/idempotency behavior. Entity CDC exists, but full event-plane journey needs release-grade E2E.                                                                                                 | Add real event store E2E: append → tail → checkpoint → replay → DLQ on failure → tenant isolation.                           |
| DC-P1-010 | Data-Cloud Security    | `DataCloudSecurityFilter.java`, `RouteSecurityRegistry.java`, route tests                                                 | Add route-level behavioral tests for every `SENSITIVE` and `CRITICAL` route. Security filter has metadata fail-closed, tenant mismatch denial, policy denial, and blocking critical audit behavior.                                                      | Generated test matrix validates auth, tenant, policy, access level, and audit behavior for each route in manifest.           |
| DC-P1-011 | Data-Cloud Security    | `DataCloudSecurityFilter.java`                                                                                            | Make audit-only mode impossible in production-like profiles unless explicitly approved by runtime profile policy. Audit-only currently allows failed auth/authorization to pass when `enforcing=false`.                                                  | Production-profile test with `enforcing=false` must fail startup unless an explicit canary/audit-only profile is configured. |
| DC-P2-012 | Data-Cloud Query       | `EntityCrudHandler.parseOffset`, query parser                                                                             | Return HTTP 400 for invalid `offset` instead of silently coercing invalid offset to 0. Current parser returns 0 on parse failure.                                                                                                                        | Tests for negative, non-numeric, huge offset, valid offset.                                                                  |
| DC-P2-013 | Data-Cloud Query       | `EntityCrudHandler.parseSorts`, `parseFilters`                                                                            | Validate sort field names and sort directions. Current sort parsing treats any non-`asc` token as descending and does not validate field names.                                                                                                          | Tests for unknown direction, invalid field, injection-like field, valid multi-sort.                                          |
| DC-P2-014 | Data-Cloud Query       | `EntityCrudHandler.parseFilters`                                                                                          | Support escaped/encoded values containing `:` or explicitly reject them with clear error. Current `split(":")` loses values with colons.                                                                                                                 | Tests for `timestamp`, URL, colon-containing string, malformed filter.                                                       |

---

# AEP / Action Plane tasks

| ID         | Product        | Where                                                     | What to do                                                                                                                                                                                                                                                    | Tests / acceptance                                                                                                                                          |
| ---------- | -------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AEP-P1-001 | AEP            | `PatternSpecCompiler.java`                                | Enforce production-context compile APIs. Legacy `compile(spec)` and `compile(spec, registry)` still allow `commitSha=null` and `environment=null`.                                                                                                            | In production-like profile, legacy compile APIs fail fast or are blocked by static/runtime checker.                                                         |
| AEP-P1-002 | AEP            | `PatternSpecCompiler.validateProductionRequirements()`    | Replace comment-only production validation with actual call-site/runtime enforcement. Current method documents that enforcement is through code review/testing.                                                                                               | Startup/runtime test proves production profile cannot use legacy compile path.                                                                              |
| AEP-P1-003 | AEP            | `PatternSpecValidator.java`, runtime lifecycle services   | Add full lifecycle E2E: draft → candidate → shadow → recommended → approved → active/predictive → degraded → rollback → retired. Validator now defines the lifecycle graph.                                                                                   | E2E asserts legal transitions succeed, illegal transitions fail, and audit/evidence records exist.                                                          |
| AEP-P1-004 | AEP            | `PatternSpecValidator.validateLifecycle`                  | Move promotion governance requirements out of nested `lifecycle.governance` only, or support both root `governance` and lifecycle-scoped governance consistently. Validator validates root governance separately and promotion governance inside lifecycle.   | Tests prove production promotion fails unless approval, evidence store, owner, risk, rollback, audit, and commit binding are present in canonical location. |
| AEP-P1-005 | AEP            | `PatternSpecCompiler.compileExpression`                   | Tighten side-effect policy requirements. `hasProductionPolicy` currently accepts approval OR review OR commitSha; side-effecting production capability should require approval/review, tool policy, audit policy, idempotency, rollback/compensation.         | Tests fail side-effecting capability without all required controls.                                                                                         |
| AEP-P1-006 | AEP/EventCloud | `products/data-cloud/planes/action/event-bridge/**`       | Add deterministic replay/checkpoint/DLQ E2E between Data-Cloud Event Plane and AEP EventCloud bridge.                                                                                                                                                         | Test: append events → bridge consumes → pattern matches → checkpoint → replay produces same decision path → DLQ on poison event.                            |
| AEP-P2-007 | AEP            | `products/data-cloud/planes/action/operator-contracts/**` | Add compile/runtime parity tests for `PatternRuntimeNode` DAG order, nested operators, schema compatibility, capability role mismatch, unknown capabilityRef, and shadow side-effect denial.                                                                  | Golden tests plus runtime tests through actual registry and operator runtime.                                                                               |

---

# Agent runtime tasks

| ID           | Product      | Where                                                   | What to do                                                                                                                                                                                                            | Tests / acceptance                                                                                                     |
| ------------ | ------------ | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| AGENT-P1-001 | Agents       | `AgentEventOperatorCapabilityAdapter.java`              | Remove or strictly gate deprecated `process(Event)` bridge before production. It is marked temporary and deprecated, but still a production method.                                                                   | Static/check test fails production code that calls deprecated bridge outside approved compatibility adapter.           |
| AGENT-P1-002 | Agents       | `AgentEventOperatorCapabilityAdapter.java`              | Persist/emit audit evidence for every invocation, including success, failure, timeout, pending approval, guardrail denial, and policy denial. Adapter records counters/evidence but persistence path must be proven.  | Runtime test asserts audit/evidence sink receives event for every outcome.                                             |
| AGENT-P1-003 | Agents       | `AgentEventOperatorCapabilityAdapter.java`              | Add real durable memory policy tests. Constructor requires durable memory unless `allowNoOpMemoryStore`, but product tests still use no-op stores.                                                                    | Production-path test uses durable memory store and fails when no-op memory is used without explicit local/test policy. |
| AGENT-P1-004 | Agents/YAPPC | `products/yappc/core/yappc-services/**`                 | Add governed agent E2E without `AiRuntimeMode.STUB`, `MemoryStore.noOp()`, mocked registry, or mocked state repository. Current governed runtime tests use stubs/mocks.                                               | Test proves capabilityRef resolution, durable state, approval/idempotency/audit, failure path, and replay marker.      |
| AGENT-P1-005 | Agents       | `scripts/audit-agent-usage.mjs`, agent runtime packages | Strengthen direct-bypass detection. Direct `TypedAgent` execution should be impossible outside approved adapter/factory/governed dispatcher paths.                                                                    | Static check fails new production calls to direct agent execution or direct tool execution bypass.                     |
| AGENT-P2-006 | Agents       | `AgentEventOperatorCapabilityAdapter.java`              | Add guardrail/policy-denial behavior paths if the counters exist. Adapter has `guardrailDeniedCount` and `policyDeniedCount`; ensure actual result mapping increments and emits those states.                         | Unit/runtime tests cover guardrail denial, policy denial, pending approval, timeout, failure, success.                 |

---

# YAPPC / Data-Cloud / Kernel tasks

| ID           | Product          | Where                                                                                                  | What to do                                                                                                                                                                                                                                                                                                            | Tests / acceptance                                                                                                   |
| ------------ | ---------------- | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| YAPPC-P1-001 | YAPPC/Data-Cloud | `products/yappc/infrastructure/datacloud/src/test/java/.../YappcDataCloudContractIntegrationTest.java` | Keep current test as contract/unit coverage, but add a real Data-Cloud integration test. Current test mocks `DataCloudClient`.                                                                                                                                                                                        | New test starts/uses real Data-Cloud API/storage/security path and proves tenant isolation server-side.              |
| YAPPC-P1-002 | YAPPC/Data-Cloud | `products/yappc/infrastructure/datacloud/**`                                                           | Persist YAPPC artifact metadata, project/workspace context, generated contracts, and artifact evidence through canonical Data-Cloud endpoints, not only `DataCloudClient` mock calls. Current test verifies call shape and tenant-specific mocked query behavior.                                                     | E2E proves route/API → Data-Cloud persistence → event/audit → read/query by tenant.                                  |
| YAPPC-P1-003 | YAPPC Agents     | `products/yappc/core/yappc-services/**`, `products/yappc/core/agents/**`                               | Replace stub-only governed agent proof with real production mode path. Current test uses `AiRuntimeMode.STUB`, no-op memory, and mocked repositories.                                                                                                                                                                 | E2E uses actual agent registry/state repository/memory policy and validates failure/degraded behavior.               |
| YAPPC-P1-004 | YAPPC/Kernel     | `products/yappc/kernel-bridge/**`, `products:yappc:kernel-bridge`                                      | Prove YAPPC uses Kernel bridge for product lifecycle/artifact workflows instead of parallel product-specific lifecycle code. YAPPC kernel bridge is registered as a module.                                                                                                                                           | E2E: intent → Kernel lifecycle → artifact generation → validation → Data-Cloud evidence.                             |
| YAPPC-P1-005 | YAPPC            | `scripts/check-yappc-*.mjs`, package scripts                                                           | Ensure `check:yappc-release-evidence` is included in a product-level gate that developers actually run for YAPPC hardening. Scripts exist for feature completeness, Data-Cloud contract integration, governed runtime, artifact roundtrip, kernel bridge contracts, duplicate runtime paths, and degraded workflows.  | `pnpm validate:yappc` or equivalent must include these checks, not leave them optional.                              |
| YAPPC-P2-006 | YAPPC            | `products/yappc/core/scaffold/**`, `products/yappc/core/agents/**`                                     | Add feature completeness matrix tests for all visible YAPPC app-generation journeys: prompt, plan, confirm, generate, preview, download, artifact roundtrip, and failure recovery.                                                                                                                                    | Contract + E2E tests prove no UI/API/CLI action is unwired or stub-only.                                             |
| YAPPC-P2-007 | YAPPC            | `products/yappc/**`                                                                                    | Add duplicate runtime path enforcement for Data-Cloud persistence, agent execution, artifact validation, and Kernel lifecycle.                                                                                                                                                                                        | Static check fails duplicate product-local implementations when canonical platform/Data-Cloud/Kernel services exist. |

---

# Cross-product E2E tasks

| ID           | Product                     | Where                                                                                      | What to do                                                                                                                                   | Tests / acceptance                                                                               |
| ------------ | --------------------------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| XPROD-P1-001 | Data-Cloud ↔ AEP            | `products/data-cloud/planes/event/**`, `products/data-cloud/planes/action/event-bridge/**` | Implement/prove event-to-pattern E2E: Data-Cloud event append → EventCloud bridge → PatternSpec match → derived event → audit/trace/metrics. | Real integration test, not mocks.                                                                |
| XPROD-P1-002 | AEP ↔ Agents                | `products/data-cloud/planes/action/agent-runtime/**`, `operator-contracts/**`              | Prove PatternSpec `capabilityRef` resolves to `EventOperatorCapability` and executes through governed agent runtime.                         | Test includes success, missing capability, role mismatch, side-effect denied, approval required. |
| XPROD-P1-003 | Audio-Video ↔ Data-Cloud    | `products/audio-video/**`, `products/data-cloud/**`                                        | Prove media processing results are persisted/emitted into Data-Cloud with tenant-safe metadata/evidence.                                     | E2E test for STT result stored and queryable by same tenant only.                                |
| XPROD-P1-004 | Audio-Video ↔ AEP/Agents    | `products/audio-video/**`, `products/data-cloud/planes/action/**`                          | Prove media result events can trigger AEP pattern and optional agent review/action.                                                          | E2E: audio/video result event → pattern match → agent review → governed decision/audit.          |
| XPROD-P1-005 | Kernel ↔ YAPPC ↔ Data-Cloud | `platform-kernel/**`, `products/yappc/**`, `products/data-cloud/**`                        | Prove product artifact workflow uses Kernel lifecycle and stores evidence/context in Data-Cloud.                                             | E2E: product unit intent → Kernel workflow → YAPPC artifact → Data-Cloud evidence → read/query.  |

---

# Test/gate quality tasks

| ID          | Product | Where                                                           | What to do                                                                                                                                                                                                              | Tests / acceptance                                                                                                             |
| ----------- | ------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| TEST-P1-001 | Suite   | `package.json`, `scripts/**`                                    | Separate mock-only tests from E2E/production proof. Current YAPPC tests using mocked DataCloudClient and STUB agent mode must not count as E2E completeness proof.                                                      | Scorecards classify each test as unit/contract/mock/integration/E2E.                                                           |
| TEST-P1-002 | Suite   | `package.json`                                                  | Add dedicated `check:audio-video-feature-completeness` and `check:audio-video-e2e` scripts. Root package has generic build/test for audio-video but no dedicated feature-completeness gate equivalent to YAPPC checks.  | Running audio-video validation executes STT/TTS/Vision/Multimodal launch, security, persistence, audit, and integration tests. |
| TEST-P1-003 | Suite   | `products/audio-video/modules/integration-tests/**`             | Populate Audio-Video integration-tests with real tests. The module currently wires dependencies and includes `*Test`, `*IT`, `*E2ETest` patterns.                                                                       | At least one E2E per STT/TTS/Vision/Multimodal plus one multimodal composition test.                                           |
| TEST-P2-004 | Suite   | `scripts/check-production-stubs.mjs`, allowlists                | Ensure production paths fail on STUB/NoOp/local-only implementations unless explicitly profile-gated.                                                                                                                   | Static check catches `AiRuntimeMode.STUB`, `MemoryStore.noOp`, in-memory stores, no-op adapters in production packages/tests.  |
| TEST-P2-005 | Suite   | `scripts/check-product-feature-completeness*`, product scripts  | Require every exposed route/API/SDK/UI action to map to at least one real-path test or be feature-flagged.                                                                                                              | Generated matrix: exposed feature → implementation → integration/E2E test → gate.                                              |
| TEST-P2-006 | Suite   | `scripts/check-route-entitlement-contracts.mjs`, route manifest | Extend route entitlement checks to assert behavior, not just registry presence.                                                                                                                                         | Test sends requests with missing auth, wrong tenant, wrong role, missing policy engine, audit failure.                         |

---

# Production quality cleanup / hardening tasks

| ID          | Product               | Where                                        | What to do                                                                                                                                                                                   | Tests / acceptance                                                                                                        |
| ----------- | --------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| QUAL-P2-001 | Data-Cloud            | `DataCloudHttpServer.java`                   | Continue decomposing god-server responsibilities. The server imports and wires a very large number of handlers/services directly.                                                            | Introduce composition modules/factories per plane; tests verify route registration parity.                                |
| QUAL-P2-002 | Data-Cloud            | `DataCloudHttpServer.java`                   | Move optional feature handler activation into explicit feature modules with fail-closed production config. Current comments show optional handlers activate when dependencies are non-null.  | Production profile fails if a visible route depends on a missing optional service unless route is hidden/feature-flagged. |
| QUAL-P2-003 | Audio-Video           | `products/audio-video/modules/**`            | Standardize all service observability: metrics names, trace spans, correlation IDs, error tags, tenant-safe labels.                                                                          | Tests assert metrics/traces for success and failure paths.                                                                |
| QUAL-P2-004 | Audio-Video           | `products/audio-video/modules/**`            | Add privacy/redaction policy for transcripts, synthesized text, detected objects/text, logs, traces, and metrics.                                                                            | Tests prove sensitive text/audio-derived content is not logged raw.                                                       |
| QUAL-P2-005 | Data-Cloud/AEP/Agents | `products/data-cloud/**`, `platform/java/**` | Add cross-module ArchUnit/static checks for dependency direction: Data-Cloud persistence bridge can depend on public AEP APIs, but non-action Data-Cloud must not import AEP internals.      | Static check fails forbidden dependency and duplicate abstraction.                                                        |
| QUAL-P3-006 | Suite                 | docs/checklists                              | Add developer-facing checklist mapping each P0/P1 feature to expected implementation path and tests.                                                                                         | Checklist references concrete modules and gates; no stale readiness claims.                                               |

---

# Recommended execution order

1. **Fix P0 launchers first:** `AV-P0-001`, `AV-P0-002`, `AV-P0-003`.
2. **Close Audio-Video production gaps:** `AV-P1-001` through `AV-P1-010`.
3. **Harden Data-Cloud production impossibility guarantees:** `DC-P1-001` through `DC-P1-011`.
4. **Close AEP/agent production enforcement:** `AEP-P1-*`, `AGENT-P1-*`.
5. **Replace YAPPC mock/stub proof with real E2E:** `YAPPC-P1-*`.
6. **Add cross-product E2E gates:** `XPROD-P1-*`.
7. **Improve test classification and product gates:** `TEST-P1-*`, `TEST-P2-*`.

This list intentionally avoids “generate evidence” as a task. The work is about making the **features complete, correct, wired, testable, and production-grade**.
