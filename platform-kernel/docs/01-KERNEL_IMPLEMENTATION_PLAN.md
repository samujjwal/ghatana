Below is the consolidated implementation task list from the YAPPC-only audit at commit `1f05a99c0bdf2d5ebbcac70f50aedfed0ec1fcf8`. I grouped tasks by file/change area so related changes can be implemented and verified together with minimal verification rounds.

# YAPPC Implementation Task List — Grouped for Minimal Verification

## Verification Round 1 — Backend correctness, Kernel handoff, phase packet truth

Run this round after completing Groups 1–4.

Recommended verification:

```bash
./gradlew :products:yappc:core:scaffold:api:test --tests "com.ghatana.yappc.kernel.*"
./gradlew :products:yappc:core:yappc-services:test --tests "com.ghatana.yappc.services.phase.*"
./gradlew :products:yappc:core:yappc-services:test --tests "com.ghatana.yappc.services.generate.*"
./gradlew :products:yappc:core:yappc-services:test --tests "com.ghatana.yappc.services.intent.*"
```

---

## Group 1 — Kernel ProductUnitIntent scope and contract strictness

YAPPC already delegates lifecycle-governed ProductUnit execution to Kernel through ProductUnitIntent and public Kernel contracts. The exporter now uses typed DTO records and a `ProductUnitKernelContractRegistry`, which is the right direction.  However, the request object still defaults missing `tenantId` to `local-dev-tenant`, which is unsafe for production scope correctness. 

| Task ID | Priority | File(s)                                                                                                             | What to Change                                                                                      | Change Details                                                                                                                                                                                                       | Why                                                                                                                   | Acceptance Criteria                                                                        |
| ------- | -------: | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| KRN-01  |       P0 | `products/yappc/core/scaffold/api/src/main/java/com/ghatana/yappc/kernel/ProductUnitIntentExporter.java`            | Remove production default `tenantId = "local-dev-tenant"` from `ProductUnitIntentExporter.Request`. | In `Request` constructor, replace `this.tenantId = builder.tenantId != null ? builder.tenantId : "local-dev-tenant";` with direct assignment. Let `validateRequest` fail when tenant is null/blank.                  | ProductUnitIntent must always carry real tenant/workspace/project scope. A dev default can hide bad production calls. | Export fails when tenant ID is missing. Existing valid tests pass with explicit tenant ID. |
| KRN-02  |       P0 | `ProductUnitIntentExporter.java`; related tests under `core/scaffold/api/src/test/java/com/ghatana/yappc/kernel/**` | Add explicit dev/test fixture builder if tests need a default tenant.                               | Create a test helper or fixture method that supplies `tenantId("local-dev-tenant")` explicitly. Do not keep the default in production DTO construction.                                                              | Keeps tests convenient without weakening production behavior.                                                         | Tests remain readable; production exporter has no fake default.                            |
| KRN-03  |       P1 | `ProductUnitIntentExporter.java`                                                                                    | Tighten producer correlation semantics.                                                             | Current producer correlation uses `intentId` in `ProducerDocument`. Consider adding an explicit `correlationId` request field so API/CLI handoff can pass the request correlation instead of overloading intent ID.  | Improves observability and support traceability.                                                                      | ProductUnitIntent includes both stable `intentId` and request correlation.                 |
| KRN-04  |       P2 | `ProductUnitIntentValidationService.java`                                                                           | Clean stale class comments that still describe map-first validation.                                | The service now validates canonical intent fields, scope, producer correlation, target provider via registry, etc. Update doc comments to match current typed/contract-backed behavior.                              | Reduces confusion; no behavior change.                                                                                | Comments match code behavior.                                                              |

---

## Group 2 — Phase packet backend SRP and canonical feature flag truth

`PhasePacketServiceImpl` has improved significantly: it injects runtime collaborators for platform run status, phase action authorization, required artifacts, and degraded packet construction.  It also builds a typed gate context from required/completed artifacts, evidence, governance, health, enabled flags, and project conditions.  The remaining issue is that the class still owns too much orchestration and some feature-flag/runtime fallback behavior is not clean enough for production.

| Task ID     | Priority | File(s)                                                                                                                                             | What to Change                                                                                 | Change Details                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Why                                                                                  | Acceptance Criteria                                                                                                    |
| ----------- | -------: | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| PHASE-BE-01 |       P0 | `products/yappc/core/yappc-services/src/main/java/com/ghatana/yappc/services/phase/PhasePacketServiceImpl.java`; new `PhaseGateContextFactory.java` | Extract `buildPhaseGateContext` and related helper methods into `PhaseGateContextFactory`.     | Move `buildPhaseGateContext`, `addBooleanConditionValues`, and `addCollectionConditions` out of `PhasePacketServiceImpl`. Inject factory into the service.                                                                                                                                                                                                                                                                                                      | Keeps phase packet assembly SRP-clean and makes gate context independently testable. | `PhasePacketServiceImpl` delegates gate context creation; focused tests cover context mapping.                         |
| PHASE-BE-02 |       P0 | `PhasePacketServiceImpl.java`; new `PhaseReadinessEvaluator.java`                                                                                   | Extract `calculatePhaseReadiness` and readiness helper methods into `PhaseReadinessEvaluator`. | Move readiness scoring, missing prerequisite calculation, estimate, confidence, artifact completion checks, policy/evidence checks into a collaborator. Current readiness logic is embedded in the phase service.                                                                                                                                                                                                                                               | Readiness is domain policy, not packet orchestration.                                | Readiness evaluator has direct unit tests for all phases and degraded cases.                                           |
| PHASE-BE-03 |       P0 | `PhasePacketServiceImpl.java`; existing or new `PhaseFeatureFlagProvider.java`                                                                      | Make feature flag truth explicit and canonical.                                                | `determineEnabledFlags` currently reads `enabledPhaseFlags`, `featureFlags`, `entitlements`, and tier-derived defaults.  There is also a `queryEnabledTenantFeatureFlags` helper that queries Data Cloud but is not visibly part of the packet assembly path in inspected code.  Decide one canonical approach: either pre-enrich project state before packet assembly or inject a `PhaseFeatureFlagProvider` that merges tenant/project/tier flags explicitly. | Admin feature-flag changes must deterministically affect backend readiness/actions.  | Phase packet includes tenant/project effective flags; admin flag update test proves action/readiness behavior changes. |
| PHASE-BE-04 |       P1 | `PhasePacketServiceImpl.java`; new `PhaseHealthSignalProvider.java`                                                                                 | Extract `buildHealthSignals` from `PhasePacketServiceImpl`.                                    | Move preview/generation/runtime/agent governance health construction to a provider. Current method also falls back to derived IDs when state lacks runtime IDs.                                                                                                                                                                                                                                                                                                 | Reduces service size and clarifies production-vs-dev runtime truth.                  | `PhasePacketServiceImpl` only calls provider; health provider tests cover healthy/degraded/missing ID cases.           |
| PHASE-BE-05 |       P1 | `PhaseHealthSignalProvider.java` or current `buildHealthSignals`                                                                                    | Treat missing runtime identifiers as degraded in production.                                   | Current logic derives `previewId`, `generationId`, and `runtimeId` from `projectId` and phase when missing.  In production profile, missing IDs should produce a degraded health signal with recovery details. Dev/test can still derive IDs.                                                                                                                                                                                                                   | Prevents false “healthy” status from synthetic runtime IDs.                          | Missing IDs in production mark health degraded and block unsafe advancement.                                           |
| PHASE-BE-06 |       P1 | `PhasePacketServiceImpl.java`; new `PhasePacketAssembler.java`                                                                                      | Extract final `new PhasePacket(...)` construction into assembler.                              | The service currently builds all intermediate values and constructs the packet inline.  Create a small assembler to keep packet construction consistent and easier to update when contract fields change.                                                                                                                                                                                                                                                       | Reduces churn and mistakes when packet model evolves.                                | Packet assembly has focused tests; service method becomes shorter and more readable.                                   |
| PHASE-BE-07 |       P1 | `PhasePacketServiceImpl.java`                                                                                                                       | Normalize fail-open/fail-closed behavior for completed artifacts and activity feed.            | `queryCompletedArtifacts` and `queryActivityFeed` return empty lists on exceptions.  Decide which failures are critical. For required lifecycle readiness, missing completed artifacts due to repository failure should likely create degraded blocker/evidence rather than appear as “no completed artifacts.”                                                                                                                                                 | Avoids hiding dependency failures as empty product state.                            | Repository failure creates explicit degraded signal where it affects readiness.                                        |

---

## Group 3 — Intent and generation production dependency strictness

Intent and generation services are feature-rich, but production constructors still allow silent optional behavior or default collaborators. `IntentServiceImpl` can be constructed without durable repository/evidence services and then returns empty/no-op results.   `GenerationServiceImpl` defaults to `AiHealthProvider.alwaysHealthy()` and directly constructs `GenerationAssuranceService`. 

| Task ID | Priority | File(s)                                                                                                           | What to Change                                                                      | Change Details                                                                                                                                                                                          | Why                                                                                      | Acceptance Criteria                                                                                               |
| ------- | -------: | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| SVC-01  |       P1 | `products/yappc/core/yappc-services/src/main/java/com/ghatana/yappc/services/intent/IntentServiceImpl.java`       | Split production constructor from test/dev constructor paths.                       | Make production constructor require `IntentRepository` and `IntentEvidenceService`. Move null-tolerant constructors to package-private test fixture or a `IntentServiceTestFactory`.                    | Intent feature is not complete if persistence/evidence can silently no-op in production. | Production composition cannot instantiate `IntentServiceImpl` without repository/evidence.                        |
| SVC-02  |       P1 | `IntentServiceImpl.java`                                                                                          | Replace null checks with explicit dependency behavior.                              | Remove `if (intentRepository == null)` and `if (intentEvidenceService == null)` branches from production code. If needed, dev/test uses explicit `NoopIntentEvidenceService`.                           | Makes behavior deterministic and easier to reason about.                                 | `findLatest`, `history`, capture evidence, and persistence paths always use concrete collaborators in production. |
| SVC-03  |       P1 | `products/yappc/core/yappc-services/src/main/java/com/ghatana/yappc/services/generate/GenerationServiceImpl.java` | Require explicit `AiHealthProvider` and `GenerationAssuranceService` in production. | Remove public constructor path that defaults `AiHealthProvider.alwaysHealthy()` and `new GenerationAssuranceService()`. Keep only explicit constructor for production; tests can use helper factory.    | Hidden “always healthy” defaults can mask production readiness failures.                 | Production composition provides explicit health and assurance collaborators.                                      |
| SVC-04  |       P1 | `GenerationServiceImpl.java`                                                                                      | Improve generation failure state persistence.                                       | Current `whenException` logs “Generation failed” but inspected lines do not show a persisted failed run update in that path.  Ensure failures update `GenerationRun` to failed with reason/correlation. | Users need accurate Generate/Run/Observe state after failed generation.                  | Failed generation creates failed run state with correlation and reason.                                           |
| SVC-05  |       P2 | `IntentServiceImpl.java`, `GenerationServiceImpl.java`                                                            | Align error messages with i18n/error-code contract.                                 | Backend exceptions such as “Shape is not ready for generation” should expose stable error codes to frontend, not only raw strings.                                                                      | Cleaner UI and localization.                                                             | API error envelope carries code + safe message.                                                                   |

---

## Group 4 — Kernel lifecycle truth source and production constructor safety

Kernel lifecycle ingest is much better now: it has provider abstraction, Data Cloud truth source, and rejects local filesystem provider in production.   Data Cloud truth source rejects blank/default tenants and normalizes typed records. 

| Task ID | Priority | File(s)                                                                                                                      | What to Change                                                                         | Change Details                                                                                                                                                                                          | Why                                                                    | Acceptance Criteria                                                            |
| ------- | -------: | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| KLT-01  |       P1 | `products/yappc/core/yappc-services/src/main/java/com/ghatana/yappc/kernelvisibility/KernelLifecycleEventIngestService.java` | Make local filesystem constructors visibly dev/test-only.                              | Default constructors still create local filesystem provider if not production.  Add Javadoc/deprecation warning or move local provider construction to a named factory like `forLocalDevelopment(...)`. | Prevent accidental production-like local use outside guarded profiles. | New production composition uses `KernelLifecycleTruthSource` constructor only. |
| KLT-02  |       P1 | `KernelLifecycleEventIngestService.java`; composition modules                                                                | Verify composition injects `DataCloudKernelLifecycleTruthSource` in non-local runtime. | Ensure all application modules wire the truth-source constructor, not default constructor.                                                                                                              | Boundary safety.                                                       | Startup/composition test fails if production uses local provider.              |
| KLT-03  |       P2 | `DataCloudKernelLifecycleTruthSource.java`                                                                                   | Add stronger typed fields for common health/gate/artifact/deployment attributes.       | Current truth record keeps generic `Map<String,Object> attributes`.  Introduce typed optional sub-records for known Kernel truth sections while preserving metadata extension map.                      | Reduces downstream map parsing and UI inconsistency.                   | Known fields are typed; unknown fields remain in metadata.                     |

---

## Verification Round 2 — Frontend phase UI, route UX, i18n, no-duplicate semantics

Run this round after Groups 5–7.

Recommended verification:

```bash
pnpm -C products/yappc/frontend/web type-check
pnpm -C products/yappc/frontend/web test:unit
pnpm -C products/yappc/frontend/web test:integration
pnpm -C products/yappc/frontend/web test:contract
pnpm -C products/yappc/frontend/web inventory:components:check
pnpm -C products/yappc/frontend/web inventory:routes:check
```

---

## Group 5 — Phase cockpit decomposition and shared state primitives

The eight-phase route structure is clean and explicit.  The cockpit now uses `@ghatana/i18n`, shared `ErrorState`, translated summary labels, and backend packet fields.   However, `_phaseCockpit.tsx` is still a large route component owning guards, mutation handlers, packet mapping, degraded rendering, summary rendering, and page assembly.

| Task ID       | Priority | File(s)                                                                | What to Change                                                               | Change Details                                                                                                                                                                                                              | Why                                                                           | Acceptance Criteria                                                                   |
| ------------- | -------: | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| UI-COCKPIT-01 |       P1 | `products/yappc/frontend/web/src/routes/app/project/_phaseCockpit.tsx` | Split route container from rendering and mapping logic.                      | Create `PhaseCockpitContainer.tsx`, `PhasePacketSummary.tsx`, `PhaseDegradedPacketPanel.tsx`, `PhaseRouteGuards.tsx`, and `usePhaseActionHandlers.ts`. Move current nested functions/components out of `_phaseCockpit.tsx`. | Reduces file size and cognitive load for engineers.                           | `_phaseCockpit.tsx` becomes a thin route export/container.                            |
| UI-COCKPIT-02 |       P1 | `_phaseCockpit.tsx`; new `phasePacketMappers.ts`                       | Extract packet-to-UI mapping.                                                | Move blockers/evidence/governance/activity/suggestions mapping out of route file. Current mappings are inline around packet rendering.                                                                                      | Prevents duplicate mapping behavior across phase panels.                      | Mappers have pure unit tests.                                                         |
| UI-COCKPIT-03 |       P1 | `_phaseCockpit.tsx`; new `usePhaseActionHandlers.ts`                   | Extract action mutation handlers.                                            | Move primary action, secondary action, generate review, and run post-action handlers into a hook. Current route owns all mutation behavior.                                                                                 | Keeps route rendering clean and makes action behavior independently testable. | Hook tests cover missing context, validate lifecycle preview, authenticated reviewer. |
| UI-COCKPIT-04 |       P2 | `_phaseCockpit.tsx`                                                    | Replace raw loading skeleton with shared loading primitive.                  | The loading branch uses local `animate-pulse` skeleton markup.  Replace with shared `LoadingState`/`SectionLoading` used elsewhere.                                                                                         | Consistent modern UI and accessibility.                                       | Loading state exposes accessible `role=status` and localized copy.                    |
| UI-COCKPIT-05 |       P2 | `_phaseCockpit.tsx`; shared error/denied components                    | Replace local error-card markup for missing project/workspace/access denied. | Missing project/workspace/access denied branches still use raw styled divs.  Use shared `ErrorState` or `AccessDeniedState` with consistent tone and support details.                                                       | Consistent 0-cognitive-load error states.                                     | All failure branches use shared state components.                                     |

---

## Group 6 — Replace frontend heuristic phase status logic with typed backend view models

`PhaseStatusPanels.tsx` is the biggest remaining frontend correctness/design issue. It currently derives Generate/Run/Observe/Learn/Evolve status by scanning activity text, matching keywords, and assigning labels/statuses locally.   This duplicates backend lifecycle intelligence and can create inconsistent or wrong UI.

| Task ID     | Priority | File(s)                                                                                                               | What to Change                                                  | Change Details                                                                                                                                                                                                                       | Why                                                                          | Acceptance Criteria                                                            |
| ----------- | -------: | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| UI-PANEL-01 |       P0 | Backend: `PhasePacket.java`, `PhasePacketServiceImpl.java`; Frontend: `types/phasePacket.ts`, `PhaseStatusPanels.tsx` | Add typed phase panel view models to `PhasePacket`.             | Add backend-provided structured fields like `generatePanel`, `runPanel`, `observePanel`, `learnPanel`, `evolvePanel` or a generic `phasePanels[]`. Include statuses, cards, recommendations, actions, traces, confidence, and owner. | Frontend should render lifecycle truth, not infer it from strings.           | `PhaseStatusPanels` no longer keyword-scans activity text for business status. |
| UI-PANEL-02 |       P0 | `PhaseStatusPanels.tsx`                                                                                               | Remove `GENERATION_ASSURANCE_CHECKS` keyword matching.          | Replace compile/test/security/i18n/a11y keyword detection with backend-provided assurance checks. Current labels and keywords are hardcoded.                                                                                         | Assurance status must be canonical and backend-owned.                        | Generate panel renders typed assurance check list.                             |
| UI-PANEL-03 |       P0 | `PhaseStatusPanels.tsx`                                                                                               | Remove run status keyword inference.                            | Replace `runHealthStatus`, `buildRunTimeline`, `resolveRunStatus`, `runOperationState` text scanning with typed run timeline/status from backend.                                                                                    | Run status should come from platform run truth, not activity strings.        | Run panel shows platform run status exactly from packet.                       |
| UI-PANEL-04 |       P1 | `PhaseStatusPanels.tsx`                                                                                               | Replace observe diagnostics keyword inference.                  | Move runtime-error/console-log/policy-block/load-timing/user-action categorization to backend typed diagnostics.                                                                                                                     | Reduces inconsistent UI and improves supportability.                         | Observe panel renders typed diagnostics.                                       |
| UI-PANEL-05 |       P1 | `PhaseStatusPanels.tsx`                                                                                               | Replace Learn/Evolve inferred signals with typed backend model. | Move learning recommendations, approval items, prompt signals, and evolve signals out of activity string inference.                                                                                                                  | Learn/Evolve should be first-class product features, not log text summaries. | Learn/Evolve panels render canonical packet data.                              |
| UI-PANEL-06 |       P1 | `PhaseStatusPanels.tsx`; locale catalog                                                                               | Replace hardcoded visible English with i18n/backend label keys. | Labels like `Healthy`, `Compile`, `Tests`, `Static analysis`, `Security`, `Accessibility`, `Actor`, `Time`, `Event`, `Outcome`, `Correlation ID` are hardcoded.                                                                      | Keeps UI localized and consistent.                                           | Static i18n guard covers `PhaseStatusPanels.tsx`.                              |
| UI-PANEL-07 |       P1 | `PhaseStatusPanels.tsx`; new components                                                                               | Split file into per-phase panels.                               | Create `GenerateStatusPanel.tsx`, `RunStatusPanel.tsx`, `ObserveStatusPanel.tsx`, `LearnStatusPanel.tsx`, `EvolveStatusPanel.tsx`, plus shared `PhaseSignalCard.tsx`.                                                                | Avoids a multi-phase “god component.”                                        | Each phase panel has focused tests and consistent layout.                      |
| UI-PANEL-08 |       P2 | Phase panel components                                                                                                | Apply 0-cognitive-load display rules.                           | Each phase panel should show: current status, why blocked/ready, next recommended action, owner/source, and confidence/support trace. Avoid long dense lists above fold.                                                             | Product usability and polish.                                                | Visual/component tests show consistent card hierarchy.                         |

---

## Group 7 — Product shell, admin/kernel/product-family UI consistency

Routes now include phase routes, Kernel health, product-family, and admin pages.  The package includes route inventory and component inventory checks.  Use this group to make every non-phase page visually consistent.

| Task ID     | Priority | File(s)                                                                                                  | What to Change                                                                                 | Change Details                                                                                                                                                                                             | Why                                                                                   | Acceptance Criteria                                                                   |
| ----------- | -------: | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| UI-SHELL-01 |       P1 | `products/yappc/frontend/web/src/routes/app/kernel-health.tsx`, `kernel-health-product.tsx`              | Align Kernel health pages to the same page header/card/table/timeline system as phase cockpit. | Use shared page layout, section cards, empty/error/loading states, and consistent action placement.                                                                                                        | Kernel health should feel like part of the same product, not a separate utility page. | Kernel health list/detail have same layout rhythm and action patterns as phase pages. |
| UI-SHELL-02 |       P1 | `routes/app/product-family.tsx`                                                                          | Align product-family page with product shell standards.                                        | Ensure header, tabs/cards, actions, empty/error/loading, and permissions match admin/kernel pages.                                                                                                         | Reduces cognitive load across product management surfaces.                            | Product-family route passes visual and accessibility smoke tests.                     |
| UI-SHELL-03 |       P1 | `routes/app/admin/*.tsx`                                                                                 | Standardize admin page layout.                                                                 | Prompt versions, A/B testing, feature flags, and observability should share one admin layout component with consistent title, description, primary actions, filters, table/card density, and empty states. | Beautiful, consistent modern UI.                                                      | All admin routes use shared admin page shell.                                         |
| UI-SHELL-04 |       P2 | `products/yappc/frontend/web/scripts/check-design-system-consistency.mjs`; component inventory allowlist | Reduce or justify local UI wrappers.                                                           | Use `inventory:components:check` to find local wrappers. Migrate unapproved wrappers to design-system components or document canonical source/rationale.                                                   | Enforces reuse-first and no duplicates.                                               | No unapproved primitive/component duplicate remains.                                  |
| UI-SHELL-05 |       P2 | route inventory metadata                                                                                 | Ensure all mounted routes have owner/auth/nav/feature-flag metadata.                           | Routes inventory already exists; keep it aligned with route additions.                                                                                                                                     | Reduces route drift and hidden pages.                                                 | `inventory:routes:check` passes.                                                      |

---

## Verification Round 3 — Docs and architecture consistency only

Run this after Groups 8–9. No evidence-generation tasks are included.

Recommended verification:

```bash
pnpm -C products/yappc/frontend/web inventory:routes:check
pnpm -C products/yappc/frontend/web inventory:components:check
```

---

## Group 8 — Documentation accuracy and developer guidance

README says current YAPPC maturity is high and references completed backlog progress.  `docs/ARCHITECTURE.md` still says lifecycle phases are partial/early and references older architecture language.  This inconsistency should be corrected because stale architecture docs will cause repeated audit churn.

| Task ID | Priority | File(s)                                         | What to Change                                                           | Change Details                                                                                                                                                                     | Why                                                                       | Acceptance Criteria                                                                       |
| ------- | -------: | ----------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| DOC-01  |       P2 | `products/yappc/docs/ARCHITECTURE.md`           | Update implementation reality snapshot.                                  | Replace old 2026-04-17 “partial/early” lifecycle table with current 2026-05-26 status, but keep remaining gaps honest: SRP, UI panel semantics, production defaults, docs cleanup. | Prevents engineers from treating already-completed work as still missing. | Architecture doc matches current code and backlog ledger.                                 |
| DOC-02  |       P2 | `products/yappc/README.md`                      | Temper high maturity claims with current remaining product-quality gaps. | Keep score, but add short “Current hardening focus” section: SRP decomposition, typed phase panel models, UI consistency, production default removal.                              | Sets correct expectations for next iteration.                             | README points developers to real next priorities.                                         |
| DOC-03  |       P3 | `products/yappc/docs/YAPPC_BACKLOG_PROGRESS.md` | Add “Next hardening focus” section without evidence-generation tasks.    | List tasks from this plan as code-quality/product-hardening backlog, not evidence backlog.                                                                                         | Keeps backlog actionable.                                                 | Backlog distinguishes completed evidence-backed work from next feature-quality refactors. |

---

## Group 9 — Final cleanup and consistency hardening

These tasks are lower-risk cleanup tasks that should happen after the main backend/frontend refactors.

| Task ID  | Priority | File(s)                                              | What to Change                                                                | Change Details                                                                                                                                     | Why                                                                      | Acceptance Criteria                                                          |
| -------- | -------: | ---------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| CLEAN-01 |       P2 | `PhasePacketServiceImpl.java`, related collaborators | Normalize naming around “phase”, “lifecyclePhase”, and “nextPhase.”           | Avoid assigning `lifecyclePhase = phase` blindly if backend has a canonical lifecycle state. Current packet construction sets both using `phase`.  | Avoids confusion between mounted UI phase and lifecycle execution phase. | Packet contract clearly differentiates UI phase and runtime lifecycle phase. |
| CLEAN-02 |       P2 | Frontend phase types and mappers                     | Remove local fallback heuristics once typed backend phase panel models exist. | Delete unused activity keyword scanners, status derivation helpers, and manual confidence defaults.                                                | Prevents duplicate logic from returning later.                           | No dead helper functions remain.                                             |
| CLEAN-03 |       P2 | Backend service constructors                         | Make production constructors explicit and test constructors package-private.  | Apply consistently to Intent, Generation, Kernel ingest, and phase services.                                                                       | Enforces DI discipline.                                                  | Public constructors represent production-safe dependencies only.             |
| CLEAN-04 |       P3 | Test names and fixtures                              | Rename fixtures to show dev/test-only behavior explicitly.                    | Use names like `localDevTenantFixture`, `devFilesystemKernelTruthFixture`.                                                                         | Prevents accidental copy into production code.                           | Test fixtures are self-explanatory.                                          |

---

# Minimal Verification Grouping Summary

Use **three verification rounds** only:

## Round 1 — Backend correctness and Kernel boundary

Covers:

* Group 1: Kernel ProductUnitIntent strictness
* Group 2: Phase packet backend SRP and feature flag truth
* Group 3: Intent/Generation production DI
* Group 4: Kernel lifecycle truth source

Primary goal:

* Backend remains correct and production-safe.
* Kernel handoff remains contract-driven.
* Phase packet remains fail-closed and backend-owned.

## Round 2 — Frontend feature UX, no duplicate semantics, 0 cognitive load

Covers:

* Group 5: Phase cockpit decomposition
* Group 6: Typed phase panels instead of text heuristics
* Group 7: Product shell/admin/kernel/product-family consistency

Primary goal:

* UI is simpler, beautiful, consistent, and backend-truth-driven.
* No duplicate frontend lifecycle intelligence.
* Phase pages and admin/kernel pages share consistent design patterns.

## Round 3 — Documentation and cleanup

Covers:

* Group 8: Documentation accuracy
* Group 9: Final cleanup

Primary goal:

* Docs reflect actual code state.
* No stale architecture confusion.
* No evidence-generation work added in this iteration.
