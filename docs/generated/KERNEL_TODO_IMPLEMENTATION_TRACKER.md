# Kernel TODO Implementation Tracker

Generated/updated by Codex on 2026-05-12.

## Completed in this implementation slice

| TODO | Status | Evidence |
| --- | --- | --- |
| 1 | Completed | `config/canonical-product-registry.json` is the editable product registration source, while `scripts/generate-product-registry-artifacts.mjs` validates registry obligations, generates `config/product-shape.json`, root `pnpm-workspace.yaml`, Gradle include fragments, CI matrix artifacts, package-script artifacts, and root product scripts. The generator now has non-mutating `--check` mode wired through `pnpm check:product-registry-artifacts`, and the package-script merge step also fails closed in `--check` mode. `pnpm check:product-registry-artifacts`, `pnpm check:product-workspace-registration`, `pnpm check:product-ci-matrices`, `pnpm check:product-kind-classification`, and `pnpm check:product-manifest-contracts` pass. |
| 2 | Completed | `config/canonical-product-registry.json` now separates `platform-provider`, `business-product`, `shared-service`, and `demo/example` entries, with the schema enum retaining the complete kind vocabulary including `domain-pack`. Data Cloud and YAPPC no longer carry product-only conformance gates, demo/example entries are excluded from product CI, generated CI matrices filter by kind, and `pnpm check:product-kind-classification`, `pnpm check:product-ci-matrices`, `pnpm check:product-workspace-registration`, and `pnpm check:product-manifest-contracts` pass after regeneration. |
| 3 | Completed | `pnpm check:kernel-boundaries` enforces Kernel/product dependency direction across source imports and Gradle files. The gate rejects platform imports of product packages/source paths, product-to-product imports for strict active business products, product bypass of platform public package exports, and platform Gradle dependencies on product modules. It also includes parser/path fixture assertions. `pnpm check:kernel-boundaries` passes for 4 strict business products, 13 registry entries, and 87 product packages. |
| 4 | Completed | Token-only conformance gates have been replaced on the critical contract paths with executable validators and typed harnesses. `scripts/check-data-access-contract.mjs` now runs TypeScript data-access contract tests, product-conformance data-access tests, and Java Kernel data-access tests. `scripts/check-observability-conformance.mjs` rejects source/token evidence, requires behavior-only observability evidence from `config/observability/product-observability-flows.json`, and executes the typed observability-flow plus telemetry fixture tests. `scripts/check-route-entitlement-contracts.mjs` now runs the shared route-entitlement conformance package and PHR/DMOS/FlashIt backend behavior tests. `scripts/check-bridge-compliance.mjs` now executes registry-declared Gradle bridge tests instead of matching `requiredEvidence` tokens. Passing evidence: `pnpm check:data-access-contract`, `pnpm check:observability-conformance`, `pnpm check:route-entitlement-contracts`, and `pnpm check:bridge-compliance`. |
| 5 | Completed | Removed stale undefined `contracts` loop from `scripts/check-observability-conformance.mjs`; validation now uses `config/observability/product-observability-flows.json` as the evidence model. |
| 6 | Completed | Root `pnpm-workspace.yaml` is generated from registry `pnpmPackages`, including PHR, DMOS, and FlashIt packages. |
| 7 | Completed | Bad split-path filters were removed from generated root scripts; product scripts now route through `pnpm product`. |
| 8 | Completed | Active YAML and JSON manifests now use one canonical top-level envelope with `schemaVersion`, `product`, `kind`, `capabilities`, `policies`, `surfaces`, and `runtimeServices`. `loadProductManifest` no longer carries the legacy `pack` wrapper special case, product-manifest fixtures validate canonical JSON/YAML loading, and `pnpm check:product-manifest-contracts` enforces the stricter envelope across active registry-backed manifests. |
| 9 | Completed | Finance and DMOS product-specific policy actions are namespaced; registry policy action namespaces are explicit. |
| 10 | Completed | DMOS and FlashIt resources are product-scoped; FlashIt wildcard resource was replaced with an explicit hierarchical resource. |
| 11 | Completed | Manifest-conformant registry entries now require explicit `manifestPath`, `manifestFormat`, and `buildFile`; non-manifest entries carry explicit manifest exemptions. |
| 12 | Completed | Kernel data-access context now has Java and TypeScript parity. `@ghatana/data-access-context` owns the TypeScript context contract and executable tests; `platform:java:database` exposes matching `DataAccessContext` fields for tenant, principal, correlation, request, client, audit classification, data-owner scope, idempotency key, fail-closed idempotency access, and immutable safe metadata. `DataAccessContextTest` covers canonical metadata, fail-closed required fields, and immutable metadata. `scripts/check-data-access-contract.mjs` is now an executable conformance runner across TS Kernel, product-conformance active-product snapshots, and Java Kernel tests. Passing evidence: `pnpm check:data-access-contract`, `pnpm --dir platform/typescript/data-access-context exec vitest run src/__tests__/data-access-context.test.ts`, `pnpm --dir platform/typescript/product-conformance exec vitest run src/data-access/__tests__/data-access.test.ts src/suite/__tests__/active-products.test.ts`, and `./gradlew :platform:java:database:test --tests com.ghatana.platform.database.DataAccessContextTest --no-daemon`. |
| 13 | Completed | Product route entitlement contracts now share one public type model in `@ghatana/product-shell`; generated/contract entry points re-export the same types to reduce drift. `@ghatana/product-conformance/route-entitlements` validates backend entitlement payloads structurally, DMOS consumes declarative generated route capability contracts, PHR route metadata is element-free and backend hydrated, and FlashIt uses the shared contract package directly. PHR, DMOS, and FlashIt shell wrappers now hydrate route visibility from `useProductEntitlements`, and `scripts/check-shared-product-shells.mjs` enforces backend entitlement adoption for all active UI shells. Passing evidence: `pnpm check:route-entitlement-contracts`, `pnpm check:shared-product-shells`, `pnpm --dir products/digital-marketing/ui run type-check`, `pnpm --dir products/digital-marketing/ui run test:route-contract`, `pnpm --dir products/flashit/backend/gateway exec vitest run src/routes/__tests__/entitlements.test.ts`, and `pnpm --dir platform/typescript/product-conformance exec vitest run src/route-entitlements/__tests__/route-entitlements.test.ts src/suite/__tests__/suite.test.ts`. |
| 14 | Completed | Shared `RoleHierarchy` / `RouteAccessEvaluator` helpers are active in `@ghatana/product-shell`; PHR, DMOS, and FlashIt route-access wrappers consume the shared evaluator. `scripts/check-shared-product-shells.mjs` now enforces `createRouteAccessEvaluator` adoption in each active product route access file, and `pnpm check:route-entitlement-contracts` plus product-shell tests pass. |
| 15 | Completed | Shared product-shell chrome primitives are fully adopted by the active shell wrappers: `ProductShellFooter`, `ProductHeaderUserMenu`, and `createProductRoleSelectorConfig` are required by `scripts/check-shared-product-shells.mjs` for PHR, DMOS, and FlashIt. PHR now uses the shared header user menu while preserving backend-entitled emergency actions. |
| 16 | Completed | `ProductShell` is split into `useProductShellState`, `ProductShellLayout`, and the convenience `ProductShell` wrapper, all exported from `@ghatana/product-shell`. `scripts/check-shared-product-shells.mjs` now enforces the state/rendering split and public exports, while product-shell render tests prove child content stability across sidebar, route, role, and notification updates. |
| 17 | Completed | `useStableProductShellConfig` was removed from `@ghatana/product-shell` exports and implementation. PHR, DMOS, and FlashIt use `useProductShellConfig(configParts)`, `scripts/check-shared-product-shells.mjs` enforces the current API, and an `rg` sweep confirms the deprecated helper has no remaining references in active shell sources/scripts. |
| 18 | Completed | Split PHR route metadata from React element construction. `phrRouteContracts.ts` now owns metadata-only route contracts, `phrRouteElements.tsx` attaches page elements at the UI boundary, shell navigation consumes contracts directly, and focused PHR tests assert emergency metadata remains element-free. |
| 19 | Completed | Removed DMOS runtime `toUiPath()` branching and product-local route metadata/action maps. The UI route manifest now consumes backend `dmos-route-capabilities.json` as the declarative route contract, generated route entries expose `uiPath` templates for element lookup, and the Java generator emits the same `uiPath` field. |
| 20 | Completed | Added `scripts/run-product-task.mjs` and root `pnpm product <productId> <task> [surface]` orchestration. |
| 21 | Completed | Product scaffolder emits canonical manifest envelopes, docs taxonomy files, UI route entitlement skeletons, data-access/idempotency/observability conformance fixtures, and a product-local conformance script. It now registers generated products in `config/canonical-product-registry.json` with manifest/build ownership, pnpm package declarations, surface package paths, CI gates, and conformance obligations; the golden scaffolder check verifies registry, workspace, Gradle, CI workflow, docs, UI, and conformance output and executes the generated product conformance script. |
| 22 | Completed | `MutationMetadataEnricher` in `platform:java:database` is the Kernel-owned mutation metadata primitive with product prefix, key-style, trace metadata provider, correlation ID factory, and owner-scope strategy support. PHR and Finance service bases both delegate metadata enrichment and owner-scope inference to the shared enricher while keeping only product-specific trace-context adapters and owner field lists. `pnpm check:data-access-contract`, `./gradlew :platform:java:database:test --tests com.ghatana.platform.database.MutationMetadataEnricherTest --no-daemon`, and `./gradlew :products:phr:compileJava :products:finance:compileJava --no-daemon` pass. |
| 23 | Completed | Java and TypeScript idempotency are standardized through Kernel contracts. `@ghatana/data-access-context` covers request fingerprinting, replay semantics, expiry handling, mismatch rejection, audit output, and in-memory conformance behavior; `@ghatana/product-conformance/idempotency` validates miss/replay/expired/conflict observations. `platform:java:database` provides `IdempotencyStore`, replay decisions, audit events, conflict exceptions, stable SHA-256 fingerprints, and an in-memory TTL store. Finance delegates transaction replay/expiry/conflict handling to Kernel stores including JDBC durability, and FlashIt enforces required mutation idempotency keys and duplicate audit-event replay through tested product adapters. Passing evidence: TS data-access tests, TS product-conformance idempotency/suite tests, Java Kernel idempotency tests, Finance transaction/idempotency/JDBC idempotency tests, and FlashIt data-access/idempotency tests. |
| 24 | Completed | Java Kernel input validation primitives live in `platform:java:security` through `SafeInputValidator`, covering safe identifiers, safe codes, allowed values, and finite numeric boundaries. `SafeLogRedactor` reuses the core `PiiRedactor` for free-text logs and adds structured metadata redaction by sensitive keys, nested metadata support, and sensitive-text detection. Finance and PHR product boundary wrappers now delegate generic identifier/code/allowed-value validation to the Kernel validator while retaining product-specific validators only where needed. `./gradlew :platform:java:security:test --tests com.ghatana.platform.security.validation.SafeInputValidatorTest --tests com.ghatana.platform.security.validation.SafeLogRedactorTest --no-daemon` and `./gradlew :products:phr:compileJava --no-daemon` pass. |
| 25 | Completed | Shared design-system async UI primitives are exported and tested: `AsyncStateBoundary`, `LoadingState`, `ErrorState`, `AccessDeniedState`, `FeatureUnavailableState`, `SuccessState`, and `EmptyState`. FlashIt analytics/collaboration empty states consume the shared design-system state primitive, and `pnpm check:shared-ui-state-coverage` enforces active product UI state/a11y/visual evidence from the canonical registry. `pnpm --dir platform/typescript/design-system exec vitest run src/molecules/__tests__/AsyncState.test.tsx` passes with 7 state-behavior tests. |
| 26 | Completed | Shared dashboard/content primitives are exported from `@ghatana/design-system`: `DashboardGrid`, `KpiStrip`, `DataCard`, `FilterBar`, `ChartPanel`, `ActivityLog`, `DataGrid`, `StatsDashboard`, and `DataTable`. The table/filter surface now associates filter labels with controls and exposes stable row keys for testing. `pnpm --dir platform/typescript/design-system exec vitest run src/organisms/__tests__/DashboardPrimitives.test.tsx src/organisms/__tests__/DataGrid.test.tsx src/organisms/__tests__/StatsDashboard.test.tsx` passes with 48 component tests. |
| 27 | Completed | `useProductEntitlements` is the active runtime entitlement boundary for all active product shells. PHR renders emergency actions only from backend entitlement payloads; DMOS now requests `/v1/route-entitlements`, hydrates backend-approved template routes, then resolves workspace paths after filtering; FlashIt now requests `/api/entitlements/route-entitlements` with the stored auth token and renders only server-approved route visibility. The shared shell check enforces hook adoption so product shells cannot drift back to local-only route disclosure. Passing evidence: `pnpm check:shared-product-shells`, `pnpm check:route-entitlement-contracts`, `pnpm --dir platform/typescript/product-shell exec vitest run src/__tests__/product-shell.test.tsx`, `pnpm --dir products/digital-marketing/ui run type-check`, `pnpm --dir products/digital-marketing/ui run test:route-contract`, and `pnpm --dir products/flashit/backend/gateway exec vitest run src/routes/__tests__/entitlements.test.ts`. |
| 28 | Completed | Finance surface truth is explicit and generated: registry surfaces now carry `implementationStatus`, generated `product-shape.json` exposes Finance `surfaceStatuses`, the Finance manifest records matching backend-only/non-UI status evidence, and the README clarifies that portal/operator are backend-only Gradle modules rather than UI packages. `pnpm check:product-manifest-contracts`, `pnpm check:product-workspace-registration`, and `pnpm check:product-ci-matrices` pass after regeneration. |
| 29 | Completed | PHR no longer hardcodes the emergency-access review action in the shell. The shell hydrates route/action entitlements from the PHR backend endpoint, renders `Emergency Access Review` only when `break-glass-review` is returned, and hides the emergency route/action for patient-role entitlements. Backend entitlement tests cover patient denial and clinician visibility. |
| 30 | Completed | FlashIt gateway imports route contracts directly from `@flashit/shared`; deprecated `products/flashit/backend/gateway/src/routes/route-manifest.contract.ts` was deleted. |
| 31 | Completed | Bridge compliance contracts are declared in `config/canonical-product-registry.json` under `conformance.bridgeAdapters`; `scripts/check-bridge-compliance.mjs` loads those contracts instead of hardcoding a DMOS-only expected test map. |
| 32 | Completed | Secret/default credential scanner policy now lives in `config/security-secret-scan.json`; `scripts/check-secret-default-credentials.mjs` loads include roots, allowlists, exclusions, names, extensions, and patterns from config. |
| 33 | Completed | Replaced broad `frontend/` production scan exclusion with precise generated/build frontend exclusions in `config/production-critical-scopes.config.json`. |
| 34 | Completed | Observability flow schema is enforced from `config/observability/product-observability-flows.schema.json`, TypeScript contracts validate the manifest in `@ghatana/product-conformance/observability-flows`, and Java parity is active through `ObservabilityFlowContract`, which loads the canonical JSON manifest into typed `Flow`, `Evidence`, `FlowKind`, and `EvidenceType` contracts. Focused Java tests cover canonical manifest loading, required facets, behavior evidence, product coverage, bridge coverage, and schema-version rejection. |
| 35 | Completed | Executable telemetry conformance now includes named product flow fixtures for PHR appointment create, Finance transaction process, DMOS API bootstrap, and FlashIt moment read/write. `pnpm check:observability-conformance` requires behavior telemetry evidence for every active observability product, and the telemetry test harness executes all required product fixtures against trace, tenant context, metrics, audit, safe logging, and redaction facets. |
| 36 | Completed | Conformance logic is now covered by testable Kernel modules instead of untested standalone token scripts. `@ghatana/product-conformance` owns typed validators and executable suites for schema envelopes, runtime conformance, route entitlements, data access, idempotency, observability flows, telemetry fixtures, and active-product suite execution; `@ghatana/product-manifest-contracts` owns manifest parsing/validation. The remaining root `check-*` files for data access, observability, route entitlement, bridge, docs, and manifest contracts are thin CLI wrappers around typed validators or executable tests. Added missing schema/runtime tests and removed the last source-token observability schema path. Passing evidence: `pnpm --dir platform/typescript/product-conformance exec vitest run`, `pnpm --dir platform/typescript/product-conformance exec tsc --noEmit`, `pnpm --dir platform/typescript/product-manifest-contracts test`, `pnpm check:data-access-contract`, `pnpm check:observability-conformance`, `pnpm check:route-entitlement-contracts`, and `pnpm check:bridge-compliance`. |
| 37 | Completed | `@ghatana/product-conformance/suite` provides a reusable Kernel conformance runner with typed conformance areas, suite/check/result contracts, duplicate check-id validation, fail-closed thrown-check handling, canonical manifest envelope checks, structural data-access context snapshot checks, idempotency observation checks, structural route entitlement payload checks, typed observability flow manifest checks, and executable telemetry fixture checks. Active-product coverage is now executable: PHR, Finance, DMOS, and FlashIt each run through the Kernel conformance suite in `src/suite/__tests__/active-products.test.ts` with product-specific manifest, data-access, idempotency, route-entitlement, observability, and telemetry fixture contexts. New scaffolded products also receive matching conformance fixture files. Passing evidence: `pnpm --dir platform/typescript/product-conformance exec vitest run src/suite/__tests__/active-products.test.ts src/suite/__tests__/suite.test.ts` and `pnpm --dir platform/typescript/product-conformance exec tsc --noEmit`. |
| 38 | Completed | `scripts/resolve-affected-products.mjs` and `pnpm check:affected-products` compute affected active registry entries from product paths, pnpm package globs, Gradle-backed Java platform changes, TypeScript/UI platform changes, global registry/build config changes, observability config, and docs-only changes. `.github/workflows/affected-products.yml` emits matrix-ready JSON, and `.github/workflows/product-isolated-ci.yml` now uses the resolver instead of duplicated `dorny/paths-filter` product path filters. `scripts/check-product-ci-matrices.mjs` enforces resolver adoption for product-isolated CI. |
| 39 | Completed | Added product-shell render performance tests that prove product child content remains stable while sidebar collapse state, role-filtered navigation, route updates, and notification inputs change. |
| 40 | Completed | Workspace dependency policy is centralized through `config/workspace-dependency-policy.json`, generated pnpm `catalog:` entries in `pnpm-workspace.yaml` and `config/generated/pnpm-workspace-entries.yaml`, and `scripts/check-platform-package-governance.js` enforcement for package-manager, root override/catalog consistency, and active Kernel/product package catalog usage. Active TypeScript Kernel/product packages have migrated shared React/Vite/Vitest/Tailwind-style dependencies to catalog references, and `pnpm-lock.yaml` is current. `pnpm check:platform-package-governance`, `pnpm check:duplicate-packages`, and `pnpm install --frozen-lockfile --ignore-scripts` pass. |
| 41 | Completed | Removed build-file inference fallback from manifest contract checks; registry must declare build ownership explicitly. |
| 42 | Completed | Canonical Kernel/product responsibility docs are the enforced source of truth: `docs/kernel/KERNEL_PRODUCT_BOUNDARY.md`, `docs/kernel/KERNEL_CONSUMPTION_GUIDE.md`, `docs/kernel/PRODUCT_MANIFEST_SPEC.md`, and `docs/kernel/PRODUCT_CONFORMANCE_SPEC.md` are linked from `docs/kernel/INDEX.md`, while `docs/implementation/README.md` now points readers to the canonical docs and generated tracker instead of duplicating the implementation catalog. Added `config/documentation-truth-scope.json` so doc truth gates validate canonical Kernel docs, generated evidence, and active product taxonomy docs while treating historical architecture/audit/session material as archived reference. Fixed current FlashIt operations doc absolute local paths. Passing evidence: `pnpm check:doc-truth`, `pnpm check:doc-claims-evidence`, and `pnpm check:product-doc-taxonomy`. |
| 43 | Completed | Added `pnpm check:cleanup-gate` and wired it into the Kernel/product boundary audit. The cleanup gate now loads production-critical roots from `config/production-critical-scopes.config.json`, scopes scanning to production-facing source, avoids generated/test/docs prompt noise, and fails stale compatibility/TODO cleanup comments in critical code. Removed the FlashIt production migration TODO in `products/flashit/backend/gateway/src/lib/performance.ts` by replacing it with current Prisma Client Extensions rationale. Passing evidence: `pnpm check:cleanup-gate` and `pnpm check:platform-package-governance`. |
| 44 | Completed | Added `scripts/generate-capability-matrix.mjs`, `pnpm generate:capability-matrix`, and `pnpm check:capability-matrix`. `docs/generated/CAPABILITY_MATRIX.md` now includes product-to-capability rows with adapter/runtime evidence, tests/conformance evidence, and conformance status from the registry and manifests. |

## Explicitly deferred broader migrations

All tracked TODOs in this slice are now completed with passing focused evidence. Broader repository-wide typecheck and production-stub cleanup remain outside this TODO tracker and are recorded under known blockers below.

## Validation commands

- `node scripts/generate-product-registry-artifacts.mjs`
- `pnpm check:product-registry-artifacts`
- `pnpm check:product-kind-classification`
- `pnpm check:doc-truth`
- `pnpm check:doc-claims-evidence`
- `pnpm check:product-doc-taxonomy`
- `pnpm check:product-workspace-registration`
- `pnpm check:product-ci-matrices`
- `pnpm check:observability-conformance`
- `./gradlew :platform:java:observability:test --tests com.ghatana.platform.observability.contract.ObservabilityFlowContractTest --no-daemon`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/telemetry/__tests__/telemetry.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec tsc --noEmit`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/telemetry/__tests__/telemetry.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/telemetry/__tests__/telemetry.test.ts src/suite/__tests__/suite.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/route-entitlements/__tests__/route-entitlements.test.ts src/suite/__tests__/suite.test.ts src/telemetry/__tests__/telemetry.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/suite/__tests__/active-products.test.ts src/suite/__tests__/suite.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec tsc --noEmit`
- `pnpm --dir platform/typescript/product-conformance exec vitest run`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/data-access/__tests__/data-access.test.ts src/route-entitlements/__tests__/route-entitlements.test.ts src/suite/__tests__/suite.test.ts src/telemetry/__tests__/telemetry.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/idempotency/__tests__/idempotency.test.ts src/data-access/__tests__/data-access.test.ts src/route-entitlements/__tests__/route-entitlements.test.ts src/suite/__tests__/suite.test.ts src/telemetry/__tests__/telemetry.test.ts`
- `pnpm --dir platform/typescript/data-access-context exec vitest run src/__tests__/data-access-context.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/idempotency/__tests__/idempotency.test.ts src/suite/__tests__/suite.test.ts`
- `pnpm --dir platform/typescript/product-conformance exec vitest run src/observability-flows/__tests__/observability-flows.test.ts src/telemetry/__tests__/telemetry.test.ts src/suite/__tests__/suite.test.ts`
- `pnpm install --lockfile-only --ignore-scripts`
- `pnpm check:platform-package-governance`
- `pnpm check:bridge-compliance`
- `pnpm check:cleanup-gate`
- `pnpm check:product-manifest-contracts`
- `pnpm --dir platform/typescript/product-manifest-contracts test`
- `pnpm --dir platform/typescript/product-shell exec tsc --noEmit`
- `pnpm --dir platform/typescript/product-shell exec vitest run src/__tests__/product-shell.test.tsx`
- `pnpm --dir platform/typescript/data-access-context exec tsc --noEmit`
- `pnpm --dir platform/typescript/data-access-context exec vitest run src/__tests__/data-access-context.test.ts`
- `pnpm --dir platform/typescript/data-access-context run build`
- `./gradlew :platform:java:database:test --tests 'com.ghatana.platform.database.idempotency.*' --no-daemon`
- `./gradlew :products:finance:test --tests com.ghatana.finance.service.TransactionServiceTest --tests com.ghatana.finance.service.TransactionProcessingIdempotencyStoreTest --no-daemon`
- `./gradlew :products:finance:test --tests com.ghatana.finance.service.TransactionServiceTest --tests com.ghatana.finance.service.TransactionProcessingIdempotencyStoreTest --tests com.ghatana.finance.service.JdbcTransactionProcessingIdempotencyStoreTest --no-daemon`
- `pnpm --dir products/flashit/backend/gateway exec vitest run src/lib/__tests__/data-access-context.test.ts src/lib/__tests__/idempotency.test.ts`
- `pnpm --dir products/flashit/backend/gateway exec vitest run src/lib/__tests__/data-access-context.test.ts`
- `./gradlew :platform:java:security:test --tests com.ghatana.platform.security.validation.SafeInputValidatorTest --no-daemon`
- `./gradlew :platform:java:security:test --tests com.ghatana.platform.security.validation.SafeLogRedactorTest --tests com.ghatana.platform.security.validation.SafeInputValidatorTest --no-daemon`
- `./gradlew :products:phr:compileJava :products:finance:compileJava --no-daemon`
- `./gradlew :products:finance:test --tests com.ghatana.finance.service.TransactionInputSanitizationUtilsTest --no-daemon`
- `./gradlew :platform:java:database:test --tests com.ghatana.platform.database.MutationMetadataEnricherTest --no-daemon`
- `./gradlew :products:phr:compileJava --no-daemon`
- `pnpm check:shared-ui-state-coverage`
- `pnpm --dir platform/typescript/design-system exec vitest run src/molecules/__tests__/AsyncState.test.tsx`
- `pnpm --dir platform/typescript/design-system exec vitest run src/organisms/__tests__/DashboardPrimitives.test.tsx src/organisms/__tests__/DataGrid.test.tsx src/organisms/__tests__/StatsDashboard.test.tsx`
- `pnpm check:duplicate-packages`
- `pnpm install --frozen-lockfile --ignore-scripts`
- `./gradlew :products:finance:compileJava --no-daemon`
- `pnpm check:route-entitlement-contracts`
- `pnpm check:data-access-contract`
- `pnpm check:shared-product-shells`
- `rg -n "useStableProductShellConfig" platform/typescript/product-shell/src products/phr/apps/web/src products/digital-marketing/ui/src products/flashit/client/web/src scripts || true`
- `pnpm check:shared-ui-state-coverage`
- `pnpm --dir products/digital-marketing/ui exec tsc --noEmit`
- `pnpm --dir products/digital-marketing/ui run type-check`
- `pnpm --dir products/digital-marketing/ui run test:route-contract`
- `pnpm --dir products/digital-marketing/ui exec vitest run src/__tests__/route-contracts.test.tsx src/lib/__tests__/action-permissions.test.ts`
- `./gradlew :products:digital-marketing:dm-domain-packs:validateDmosDomainPackBindings --no-daemon`
- `pnpm --dir products/phr/apps/web exec tsc --noEmit`
- `pnpm --dir products/phr/apps/web exec vitest run src/__tests__/phrApp.test.tsx`
- `./gradlew :products:phr:test --tests com.ghatana.phr.api.PhrHttpServerTest --no-daemon`
- `pnpm --dir products/flashit/client/web exec tsc --noEmit` (blocked by existing FlashIt client dependency/type backlog)
- `pnpm --dir products/flashit/backend/gateway exec vitest run src/routes/__tests__/entitlements.test.ts`
- `pnpm --dir platform/typescript/design-system exec tsc --noEmit`
- `pnpm --dir platform/typescript/design-system exec vitest run src/molecules/__tests__/AsyncState.test.tsx src/organisms/__tests__/DashboardPrimitives.test.tsx`
- `pnpm product digital-marketing build --dry-run`
- `pnpm product data-cloud build --dry-run`
- `pnpm product audio-video build --dry-run`
- `pnpm check:kernel-boundaries`
- `pnpm check:secret-default-credentials`
- `pnpm check:product-scaffolder`
- `pnpm check:affected-products`
- `node scripts/resolve-affected-products.mjs --json products/flashit/client/web/src/pages/AnalyticsPage.tsx platform/typescript/design-system/src/molecules/EmptyState.tsx docs/kernel/KERNEL_PRODUCT_BOUNDARY.md`
- `pnpm generate:capability-matrix`
- `pnpm check:capability-matrix`
- `pnpm check:product-doc-taxonomy`
- `rg -n "route-manifest\\.contract" products/flashit`

## Known unrelated blockers observed

- `pnpm typecheck:workspace` currently fails in `platform/typescript/accessibility` before reaching this slice.
- `git diff --check` currently reports trailing whitespace in pre-existing dirty data-cloud Java changes outside this slice.
- `pnpm check:production-stubs` now scans frontend paths and fails on the existing production placeholder backlog: 756 critical findings, 1442 warnings.
- `pnpm --dir products/flashit/backend/gateway exec tsc --noEmit` currently fails on existing FlashIt gateway type/dependency issues unrelated to the route-manifest re-export removal.
- `pnpm --dir products/flashit/client/web exec tsc --noEmit` currently fails on existing FlashIt client type/dependency issues; the product-shell/route lifecycle compatibility errors are cleared.
